# DB-AIOps v2.0 独立复审报告

**复审范围**：`c0ce285..9206be4`（14 个提交，36 个文件，+6050 行）
**复审日期**：2026-08-16
**复审方式**：全量跑测 + 逐行阅读 + 编写复现用例实证
**基线环境**：Python 3.11 / Django 5.2.17 / PostgreSQL 16（本地真实实例）

---

## 0. 结论摘要

**先说结论：v2.0 不能上生产。** 不是因为质量粗糙，而是因为它的三个旗舰功能
（WarRoom 阻塞拓扑、一键自愈 Playbook、Copilot/智能体检）**在关键路径上是演示态的**，
且系统会**主动声称自己成功了**。

先给出容易被误读的一面 —— **所有门禁都是绿的**：

| 检查项 | 结果 |
|--------|------|
| 后端全量测试 | 198 passed, 21 skipped |
| 前端 vitest | 12 passed |
| `manage.py check` / 迁移漂移 | 通过 |
| 红线扫描 / 密钥扫描 / 依赖完整性 | 通过 |
| 前端构建 | 通过 |

**绿灯没有说谎，但它测的不是你以为的东西。** 本次复审新写了 10 条复现用例，
在当前 master 上 **10 条全部失败**（8 failures + 2 errors），覆盖越权、谎报成功、
崩溃、误判四类问题。详见 §6 与附录 A。

问题分级：

| 级别 | 数量 | 性质 |
|------|------|------|
| **P0** | 4 | 安全越权 / 故障现场谎报成功 —— 会直接导致运维事故 |
| **P1** | 5 | 用户可见的功能性缺陷，其中 2 条是必崩 |
| **P2** | 7 | 设计不合理，会持续产生错误结论或维护债 |
| **P3** | 5 | 工程与流程缺口，是前面问题得以流入的原因 |

---

## 1. P0 —— 必须在上线前解决

### REV-01【安全】v2 接口族缺数据范围隔离，是 BUG-103 的原地复发

**定位** `monitor/api_views_v2.py:102, 164, 203, 224`

五个 v2 端点中，只有 `RealtimeActiveIncidentsView` 调了 `get_user_database_ids`。
其余四个直接按主键取对象：

```python
# api_views_v2.py:164 / 203 / 224
cfg = DatabaseConfig.objects.filter(id=config_id).first()   # 没有任何范围校验
# api_views_v2.py:102
inc = Incident.objects.select_related('config').filter(incident_id=incident_id).first()
```

**这不是本项目的惯例问题 —— 同一个仓库里就有正确写法作为对照组。**
`monitor/api_views_phase8.py:66-74` 定义了 `_get_config()` / `_get_incident()`，
每个端点都过一遍数据范围；`api_views_v2.py` 没有复用它，也没有自己实现。

**影响**（按严重度递增）

| 端点 | 越权后果 |
|------|---------|
| `warroom-context` | 读到任意实例的库名/库类型/因果推理链 |
| `blocking-graph` | 读到任意实例的会话拓扑 |
| `execute-dryrun` | 对任意实例做自愈预演 |
| **`execute-safely`** | **对任意实例执行自愈剧本 —— 越权的不是"看数据"，是"下运维动作"** |

拥有 `TICKETS_EXECUTE` 权限但数据范围只覆盖测试库的用户，可以直接对生产库下发剧本。

**实证** `V2DataScopeTests` 4 条用例，全部返回 200（期望 403/404）。

**建议** 复用 `api_views_phase8._get_config` / `_get_incident`；权限不足与不存在
统一返回 404（避免通过状态码探测实例存在性），服务端日志区分二者。这与 v1
`PerfBaseView.get_config()` 的既有处理保持一致。

---

### REV-02【安全/正确性】自愈剧本"执行"不执行任何动作，却谎报成功并写入失真审计

**定位** `monitor/playbook_engine_v2.py:108-139`

```python
        # 模拟执行成功并记录          ← 代码自己的注释
        record = PlaybookRunRecord.objects.create(
            ...
            status='success',
            execution_result={'result': 'SUCCESS', 'msg': '已成功执行应急动作', 'affected_rows': 1},
        )
        return {..., 'message': f"自愈预案 [{template.name}] 执行成功！阻塞资源已释放"}
```

全函数**没有任何一次目标库连接**（已实证：`grep DbConnector` 无匹配，
mock `DbConnector.get_connection` 后调用次数为 0）。`steps_payload` 里的
`KILL_SESSION` / `EXTEND_DATAFILE` / `FLUSH_TEMP` 三个动作码在全仓库**没有任何派发实现**。

**为什么这是 P0 而不是"功能未完成"**

功能未完成的正确表现是返回 501 或"未实现"。当前实现的表现是：

> 生产库正在被一个长事务阻塞，值班工程师在 WarRoom 点下"安全终止根源阻塞会话"，
> 界面弹出绿色提示 **"自愈预案执行成功！阻塞资源已释放"**，
> 审计记录写下 `status=success, affected_rows=1`。
> **而阻塞会话原封不动。**

工程师会据此停止排查、向上汇报已处置。这比"按钮没反应"危险一个数量级 ——
**故障处置现场的假成功会被相信，并且事后审计也查不出来，因为审计记录同样是假的。**

**实证** `PlaybookExecutionIsFakeTests`：零连接 + 返回 success。

**建议** 二选一，**不接受第三种**：
1. 实现真实下发（经 `DbConnector.get_connection(..., readonly=False)`，按方言派发 KILL 等动作），或
2. 在实现前让该端点明确返回"未实现"，前端按钮置灰并标注。

无论选哪个，`execution_result` 都不得在未真正执行时写 `SUCCESS`。

---

### REV-03【安全】WarRoom 前端硬编码 kill 目标，安全护栏因此永不触发

**定位** `frontend/src/pages/IncidentWarRoom.jsx:94-99`

```javascript
const res = await apiV2.executePlaybook({
  ...
  params: { username: 'app_trade_user', session_id: '1845' }   // 写死
});
```

无论当前是哪个事故、哪个实例、哪个会话在阻塞，前端一律上报
`session_id=1845` / `username=app_trade_user`。

**双重后果**
1. 一旦 REV-02 补上真实执行，这里会去 kill 一个**与当前故障无关的会话号**；
2. 后端的"安全熔断"检查 `PROTECTED_USERS`（root/sys/system/…）比对的正是这个
   前端传来的 `username`。由于它恒为 `app_trade_user`，**该护栏从 UI 路径永远不可能触发**。

并且执行前**没有二次确认弹窗**（`Modal` 已 import 但未用于此处），点击即执行。

**建议** 目标会话必须来自真实阻塞拓扑的用户选择；`PROTECTED_USERS` 的校验对象
应改为**服务端从目标库实际查到的会话属主**，而不是调用方自报的字符串（见 REV-11）。

---

### REV-04【正确性】阻塞拓扑接口返回完全伪造的数据

**定位** `monitor/api_views_v2.py:163-187`

`BlockingGraphView.get()` 取到 `cfg` 之后**完全不使用它**，直接返回硬编码常量：
会话 `1845`、用户 `app_trade_user`、客户端 IP `10.10.30.12`、
SQL `UPDATE trade_order SET status = 2 WHERE batch_id = 90218`、18 个被阻塞会话。

**任何实例、任何时刻，返回值完全相同。**

项目里已有真实的阻塞树实现（v1 `api_views_perf.BlockingTreeView`，含我上一轮补的
死锁环检测），v2 没有复用它。

**影响** 与 REV-02/03 串成一条完整的假闭环：
**假拓扑 → 假目标 → 假执行 → 成功提示**。四个环节单独看都像"待完善"，
连起来是一个会误导处置决策的系统。

**建议** 接入 v1 已有实现；在接入前该端点应返回明确的"未实现"，不得返回样例数据。

---

## 2. P1 —— 用户可见的功能性缺陷

### REV-05【必崩】Copilot 与一键体检：目标库只要有任意一条事故就 100% 崩溃

**定位** `monitor/copilot.py:81`

```python
'severity': inc.severity,      # Incident 没有 severity 字段
```

`Incident` 的字段是 `priority`（P1-P4）。已实证模型无 `severity` 属性。

**触发条件** `_get_database_context()` 会取该库最近 3 条 Incident。
只要**存在至少一条事故**，就抛 `AttributeError`，且调用链上无 try 兜底：

- `run_copilot_chat(config_id=X)` → 500
- `generate_quick_health_assessment(X)` → 500（`QuickAssessmentView` 只判 `'error' in assessment`，异常直接穿透）

**这个失效模式的讽刺之处**：数据库越是"有故障、真正需要智能助手"，
这两个功能越是必然崩溃；只有从没出过事故的库才能用。

**为什么现有测试没发现**：`tests_phase8.CopilotTests.setUp` 创建的实例**不含任何 Incident**。

**实证** `CopilotIncidentSeverityTests` 2 条，均 AttributeError。

**建议** 改 `inc.priority`；并给 `_get_database_context` 的各聚合段加独立兜底，
单段取数失败不应让整个助手不可用（按 `degrade.note` 留痕）。

---

### REV-06【功能】活跃故障过滤用了不存在的状态值，处理中的故障从 WarRoom 消失

**定位** `monitor/api_views_v2.py:70`

```python
qs = Incident.objects.filter(status__in=['open', 'investigating'])
```

`Incident.STATUS_CHOICES` = `open / diagnosing / plan_ready / executing / verifying / resolved / closed`。
**`investigating` 不存在**；而真正"处理中"的四个状态一个都没包含。

**影响** 故障一旦被人接手（`open → diagnosing`），立刻从"实时活跃故障"列表消失。
WarRoom 只显示无人认领的故障 —— 恰好把正在处置的故障隐藏了。

**实证** `RealtimeActiveIncidentStatusTests`：4 条处理中事故，查到 0 条。

**建议** 改为 `exclude(status__in=['resolved', 'closed'])`，语义上更稳健
（新增中间状态时不会再漏）。

---

### REV-07【正确性】体检对零数据实例编造指标并给满分

**定位** `monitor/copilot.py:212, 213, 234`

```python
cpu_usage  = float(metrics.get('cpu_usage',  metrics.get('cpu_percent', 35)))
conn_usage = float(metrics.get('conn_usage_pct', metrics.get('threads_connected', 20)))
disk_usage = float(metrics.get('disk_usage_pct', metrics.get('tablespace_used_pct', 45)))
```

采集链路从未上报过数据的实例，会被当作 CPU 35% / 连接 20% / 磁盘 45%，
于是"性能与负载"和"容量规划"两个维度**双双拿到 100 分**。

体检报告的用途就是回答"这个库健康吗"。对一个我们**一无所知**的库回答"满分"，
是最不该给的答案 —— 正确答案是"数据不足，无法评估"。

**实证** `QuickAssessmentFabricatedMetricsTests`：零数据实例，性能与容量均 100 分。

**建议** 缺数据时该维度返回 `score=None` / `unknown`，并从加权总分的分母中剔除，
在报告里显式列出"未覆盖维度"。

---

### REV-08【正确性】连接使用率单位混淆：把绝对连接数当百分比

**定位** `monitor/copilot.py:213, 220`

`conn_usage` 缺省回退到 `threads_connected`（**绝对连接数**），
随后与百分比阈值比较：`if conn_usage > 85`。

**后果** 一个连接上限 2000、当前 90 个连接（4.5%）的健康实例，
会被判定为 **"连接池接近饱和 (90%)"** 并扣 25 分。

**实证** `ConnUsageUnitConfusionTests`：`threads_connected=90, max_connections=2000`
→ 命中"连接池接近饱和"。

**建议** 只接受 `conn_usage_pct`；确需从原始值算，必须显式
`threads_connected / max_connections * 100`。

---

### REV-09【UI】Copilot 的 Markdown 全部以原文显示

**定位** `frontend/src/components/CopilotDrawer.jsx:324-326`

```jsx
<div style={{ whiteSpace: 'pre-wrap' }}>{msg.content}</div>
```

后端 system prompt 明确要求"输出 Markdown（含表格、代码高亮、重点加粗）"，
降级引擎也确实生成 `| 指标项 | 当前数值 |` 表格与 `**加粗**`。
前端按纯文本渲染，用户看到的是满屏 `**` 和 `|`。开场白第一句
`我是 **DB-AIOps 智能运维 Copilot 助手**` 就已经是这样。

**说明**：当前写法**没有 XSS 风险**（未用 `dangerouslySetInnerHTML`），
修复时请沿用仓库既有做法 —— `ReportList.jsx` 用 `DOMPurify.sanitize` + 白名单配置，
不要直接渲染原始 HTML。

---

## 3. P2 —— 设计不合理

### REV-10 RCA 3.0 的"因果推理"是关键词匹配 + 硬编码结论 + 硬编码置信度

**定位** `monitor/rca_engine_v3.py:64-87`

```python
if lock_root or 'lock' in incident.title.lower() or '阻塞' in incident.title:
    steps.append({
        'description': "长事务持有排他行锁/表锁，导致下游多个业务连接进入队列锁等待",  # 写死
        'confidence': 0.920,                                                        # 写死
        'metric_snapshot': {'wait_event': 'enq: TX - row lock contention'}          # Oracle 专有
    })
```

三个具体问题：

1. **标题里出现"锁"字就断言锁竞争**，并给出 92% 置信度。置信度是字面量，不是算出来的 ——
   这是在制造虚假精度。
2. **`enq: TX - row lock contention` 是 Oracle 专有等待事件**，却会原样输出给
   MySQL / PG / 达梦 的事故。在本项目"六种方言"的定位下这是硬伤。
3. **第 4 步 RESOURCE 节点无条件追加**（`steps.append` 不在任何 if 内），
   即：**任何事故**都会得到一条"连接池与活跃会话剧增"的结论，置信度 0.880，
   而全程**没有读取任何一个指标**。`metric_snapshot` 里放的是 `{'priority': 'P1'}` —— 不是指标。

**影响** 这是 AIOps 产品最不能出的一类问题：**系统以带数值置信度的方式陈述编造的因果结论**，
而使用者会据此决策。相比之下"没有推理能力"是诚实的，"看起来有推理能力"是有害的。

**建议** 每个节点必须绑定可回溯的证据（ASH 采样 / 锁等待边 / 指标时序），
无证据不出节点；置信度由证据强度计算；等待事件按 `config.db_type` 分方言取。

---

### REV-11 v2 执行路径绕过既有审批与自治等级机制，安全字段定义了但从不生效

**定位** `monitor/playbook_engine_v2.py` 全文

- `PlaybookTemplate.risk_level`（含 `critical = 极高风险(强制阻断)`）与
  `min_autonomy_level` —— 已实证在 models 之外**没有任何读取**，仅在响应里回显。
  一个标记为 `critical/强制阻断` 的剧本会被照常执行。
- 项目已有完整的审批机制 `monitor/approval_engine.py`
  （`initial_status = 'approved' if risk_level == 'low' else 'pending'`，含双人审批），
  以及自治等级 `monitor/autonomy_policy.py`。**v2 一个都没接**。
- 对照 v1：`api_views_perf.py:1019` 对 KILL_SESSION 的做法是创建 `risk_level='high'` 工单走审批。
  **v2 的同类动作只需 `TICKETS_EXECUTE` 权限，无工单、无审批、无自治等级判定。**

**`PROTECTED_USERS` 护栏是装饰性的**：它校验的是**调用方在请求体里自报的** `username`，
而非目标库上该会话的真实属主。调用方传任意字符串（或干脆不传 → `''`）即可绕过。

**建议** v2 执行统一走 `approval_engine`；保护账号校验改为服务端查目标库实际会话属主；
`risk_level` / `min_autonomy_level` 要么接上 `autonomy_policy.effective_level(cfg)`，要么删掉
——**定义了却不生效的安全字段比没有更危险，因为它让人以为有防护。**

---

### REV-12 `init_default_playbooks()` 在每次预演/执行时覆盖模板，抹掉运维自定义

**定位** `monitor/playbook_engine_v2.py:49-64, 77`

`evaluate_dryrun()` 第一行就调 `init_default_playbooks()`，内部对 3 个模板做
`update_or_create(defaults={...})`。

**后果** DBA 在后台把 `KILL_ROOT_BLOCKER` 的 `risk_level` 从 low 调到 high、
或改了 `steps_payload`，**下一次任何人做预演时就被静默改回默认值**。
另外每次 API 调用附带 3 次 `update_or_create`（6 次查询）的固定开销。

**建议** 种子数据放迁移或 management command，不要放在读路径上；
或至少改为 `get_or_create`（只在缺失时创建，不覆盖）。

---

### REV-13 `run_id` 可碰撞导致主键冲突

**定位** `monitor/playbook_engine_v2.py:118`

```python
run_id = f"RUN-{timezone.now().strftime('%Y%m%d%H%M%S')}-{config.id}"   # 主键
```

秒级时间戳 + config_id。同一实例同一秒内两次执行（重试、双击、并发处置）
→ 主键冲突 → `IntegrityError` → 500。

**建议** 追加 `uuid4().hex[:8]`。

---

### REV-14 GET 请求触发写入，且"先删后插"无事务保护

**定位** `monitor/api_views_v2.py:107-110` + `monitor/rca_engine_v3.py:90`

`WarRoomContextView.get()` 在因果链为空时调用推导并落库 —— **GET 产生副作用**。
而推导内部是：

```python
IncidentCauseChain.objects.filter(incident=incident).delete()   # 先全删
for s in steps:
    IncidentCauseChain.objects.create(...)                      # 再逐条插，无 transaction.atomic
```

**两个问题**
1. 插入中途失败 → 旧因果链已删、新的没建完 → **该事故的因果链彻底丢失**；
2. 两个人同时打开同一个 WarRoom → 都判定为空 → 并发删/插 →
   命中 `unique_together (incident, step_seq)` 抛 `IntegrityError`，或互相覆盖。

**建议** 推导整体包 `transaction.atomic()`；推导触发改为显式 POST（或异步任务），
GET 只读取已有结果。

---

### REV-15 `apiV2` 绕过统一 axios 实例，丢掉全部横切能力

**定位** `frontend/src/services/api.js:560-580`

5 个方法各自用裸 `axios` 并手写 `Authorization` 头，**没有走项目的 `api` 实例**。因此丢失：

| 能力 | 统一实例 | apiV2 |
|------|---------|-------|
| 401 自动清 token 跳登录 | 有 | **无** —— token 过期时 WarRoom 只报错不跳转 |
| 30s 超时 | 有 | **无** —— 请求可无限挂起 |
| 5xx 幂等重试 | 有 | 无 |
| 统一错误处理（含 BUG-116 修复） | 有 | 无 |

token 读取逻辑重复 5 份。

**建议** 改用 `api` 实例；`/api/v2` 前缀用第二个 `axios.create({baseURL:'/api/v2'})`
共享同一套拦截器。

---

### REV-16 业务常量硬编码

- `api_views_v2.py:86` SLA 写死 `900` 秒，且对所有事故等级一视同仁；
- `api_views_v2.py:122-135` `recommended_actions` 写死两个剧本，与事故类型、
  数据库方言均无关（`FLUSH_QUERY_CACHE` 只适用于 mysql/pgsql，却会推荐给 Oracle 事故）。

按仓库红线，新增可调项应登记到 `monitor/appconf.py` 的 SPECS。

---

## 4. P3 —— 工程与流程缺口（这些是前面问题得以流入的原因）

### REV-17 43 条契约断言不在任何自动化路径中

**定位** `monitor/tests_phase7.py` + `scripts/validate.sh:81-83`

`tests_phase7.py` 有 30 条 `wait_class` 映射 + 12 条 SQL 指纹 + 5 条边界断言，
但它们写在 `def run()` 里、由 `if __name__ == '__main__'` 驱动，**不是 TestCase**。
`manage.py test` 会 import 这个模块但收集到 **0 条用例**。

唯一的执行入口 `verify_phase7.py` 需要 `.env` + 活的 runserver + 哨兵进程 + docker 容器组，
**在干净检出下第一行就崩**（已实证：`FileNotFoundError: '.env'`），CI 里更不可能跑。

覆盖的恰恰是 AAS/性能中心图表的 `wait_class` 分类与 SQL 聚合指纹 —— 改坏了没人知道。
（手工执行确认当前 **43/43 通过**，即断言本身是好的，只是没人跑。）

`validate.sh` 的注释说"不在自动发现范围内" —— 更准确的说法是：
**它在发现范围内，只是不产生任何用例**。

**建议** 用 `subTest` 包成 TestCase（约 10 行），纳入 unit 层。

---

### REV-18 红线扫描器未接入 CI，且只扫未提交的改动 —— 对已推送代码永远无效

**定位** `scripts/lint_redlines.py:45-56`、`.github/workflows/ci.yml`

1. `_changed_files()` 基于 `git diff HEAD`。CI 是干净检出 → **扫描集为空 → 必然通过**。
   已实证：干净工作区下 `_changed_files()` 返回 `[]`。
2. 更关键：**它根本没有接入 CI**（只在 `validate.sh unit` 里，即本地/pre-commit）。
   而 pre-commit 是本地文件、不随仓库分发、对云端 Agent 无效 —— 这一点
   `scripts/pre-commit` 的注释自己写得很清楚。

**后果**（实证）v2.0 带入了 3 处违反本仓库红线的静默吞异常：

| 位置 | 代码 | 危害 |
|------|------|------|
| `copilot.py:57` | `except Exception: ctx['latest_metrics'] = {}` | 指标解析失败被当成"没有指标"，进而触发 REV-07 的编造默认值 |
| `api_views_phase8.py:454` | `except Exception: pass` | 规则名映射静默丢失 |
| `api_views_phase8.py:504` | `except Exception: pass` | **熔断器状态读取失败时报 `circuit_open=False`（fail-open）** |

其中第三条值得单列：读不到熔断状态就回答"没熔断"，是在安全信号上 fail-open。

**建议** 把 `lint_redlines.py` 加入 CI static job；CI 模式下扫描范围改为
`git diff origin/master...HEAD`（push 场景用 `HEAD~1..HEAD`），而不是工作区 diff。

---

### REV-19 v2 视图层零测试覆盖，且现有 v2 测试把伪造行为写成了期望值

**实证**：全仓库 `grep "api/v2"` 在测试中**零命中** —— 5 个 v2 端点没有任何
HTTP 层用例，RBAC、参数校验、错误码全部未覆盖。

更需要注意的是已有的两条 v2 测试**固化了缺陷**：

```python
# tests_phase8.py:478 —— 把"不执行任何动作却返回 success"断言成正确行为
self.assertEqual(exec_res['status'], 'success')

# tests_phase8.py:425 —— 这条能过，正是因为 REV-07 的编造默认值把分数抬上去了
self.assertGreaterEqual(assessment['overall_score'], 80)
```

**这类测试比没有测试更糟**：它让缺陷看起来"已被验证"，并且将来有人修正 REV-02/07 时，
这两条测试会转红，从而把**修复**误判成**回归**。

**建议** 补 v2 视图层用例（尤其数据范围矩阵）；上述两条断言应随 REV-02/07 的修复一并改写。

---

### REV-20 Django 版本无上界，迁移由 6.0.5 生成而 CI 跑 5.2.17

`requirements.txt` 写的是 `Django>=5.2.0`（无上界），
而 `0022` / `0023` 两个迁移文件头写着 `# Generated by Django 6.0.5`，
本地与 CI 解析到的却是 5.2.17 —— **开发环境与 CI 跑在不同的大版本上**。
Django 6.0 相对 5.2 有破坏性变更，这种漂移迟早会以"本地能跑、CI 炸"或反过来的形式爆发。

**建议** 收敛为 `Django>=5.2,<6.0`，并统一开发环境。

### REV-21 其余观察

- `api_views_v2.py` 的 POST 端点 `csrf_exempt`，而 `require_auth` 支持从
  Cookie `auth_token` 取凭据。当前前端只用 `Bearer`（localStorage），Cookie 分支无人使用，
  因此**暂无实际可利用性**；但只要将来有人启用 Cookie 认证，这批执行类端点会立刻变成 CSRF 目标。
  建议移除 Cookie 回退，或对写操作强制 CSRF。
- `PlaybookRunRecord.operator` 是自由文本而非 User 外键，审计可追溯性弱于 v1 的 `AuditLog`。
- 审计中间件按 `/databases/(\d+)/` 从 URL 提取实例 ID，而 v2 执行端点的 `config_id` 在**请求体**里，
  因此 `execute-safely` 的审计记录 `config=None` —— **审计日志无法回答"动了哪个库"**。
- `PlaybookRunRecord.STATUS_CHOICES` 定义了 7 个状态（含 `approved`/`rolled_back`），
  实际只会写入 `success`；状态机是纸面上的。
- v2.0 设计文档（`docs/v2.0/`，1099 行）描述的能力与实现存在系统性差距，
  尤其 §阻塞拓扑与自愈执行。建议文档中对未实现部分显式标注状态。

---

## 5. 一个整体判断

单看每条问题，都像"新功能还没打磨完"。但把 P0 的四条连起来看，是一条完整的链：

```
   假的阻塞拓扑（REV-04）
        ↓  操作员据此判断该 kill 谁
   写死的 kill 目标（REV-03）
        ↓  安全护栏比对的是这个写死值，永不触发
   不执行任何动作的"执行"（REV-02）
        ↓
   "自愈预案执行成功！阻塞资源已释放" + 审计记录 SUCCESS
```

**这条链上没有任何一环会报错。** 全绿的 CI、通过的测试、成功的提示、干净的审计 ——
系统在每一层都表现得像它工作正常。

对监控与运维工具来说，**"错误地宣称成功"比"明显地失败"危险得多**：
后者会被立刻发现，前者会在最需要可靠信息的时刻（生产故障处置现场）
让人做出错误决策，并且事后复盘时因为审计记录同样失真而查不出来。

因此我的建议不是"逐条修完再发"，而是**先把所有"假装成功"的路径改成诚实的"未实现"**
（REV-02/03/04，改动量很小），让 v2.0 可以安全地以"部分功能预览"的形态存在；
然后再按 P0→P1→P2 的顺序补齐真实能力。诚实的半成品可以上线，会说谎的成品不行。

---

## 6. 修复优先级与验收

| 顺序 | 条目 | 理由 |
|------|------|------|
| 1 | REV-01 数据范围 | 安全越权，改动小（复用现成 helper） |
| 2 | REV-02/03/04 | 先改成诚实的"未实现"，切断假成功链 |
| 3 | REV-05 | 必崩，一行修复（`inc.severity` → `inc.priority`） |
| 4 | REV-06/07/08 | 用户可见的错误结论 |
| 5 | REV-18/17/19 | 补上门禁，否则同类问题会继续流入 |
| 6 | REV-10/11 | 推理与安全机制的实质化，工作量最大 |

**验收标准**：本次复审附带的 `monitor/tests_v2_review.py`（10 条用例）
在修复后应全部转绿。它们当前 **10 条全部失败**，每条对应上面一个条目。

> ⚠️ 该文件**尚未提交**。它现在是故意失败的，直接推到 master 会让 CI 变红
> （本仓库直推 master、CI 是事后信号，红了就是真红）。
> 建议随第一批修复一起提交，届时它从"缺陷复现"自然变成"回归防线"。
> 文件已放在工作区，内容见附录 A。

---

## 附录 A：复现用例执行结果

```
$ DJANGO_SETTINGS_MODULE=dbmonitor.settings_test_unit \
  python manage.py test monitor.tests_v2_review -v 2

ERROR: test_database_context_survives_incident      → AttributeError: 'Incident' object has no attribute 'severity'
ERROR: test_quick_assessment_survives_incident      → AttributeError: 'Incident' object has no attribute 'severity'
FAIL:  test_blocking_graph_rejects_out_of_scope_database   → 200 not found in (403, 404)
FAIL:  test_dryrun_rejects_out_of_scope_database           → 200 not found in (403, 404)
FAIL:  test_execute_rejects_out_of_scope_database          → 200 not found in (403, 404)
FAIL:  test_warroom_rejects_out_of_scope_incident          → 200 not found in (403, 404)
FAIL:  test_execute_must_not_claim_success_without_doing_anything
                                                    → 一次都没连接目标库却返回 success
FAIL:  test_no_metrics_must_not_yield_full_marks    → 100 == 100（零数据实例拿满分）
FAIL:  test_absolute_connection_count_is_not_a_percentage
                                                    → 90/2000=4.5% 被判为"连接池接近饱和"
FAIL:  test_in_progress_incidents_must_stay_active  → 0 != 4（处理中事故全部漏掉）

Ran 10 tests — FAILED (failures=8, errors=2)
```

## 附录 B：本次复审执行的验证

| 验证 | 结果 |
|------|------|
| `manage.py test monitor` | 198 passed / 21 skipped |
| `manage.py test monitor --shuffle` | 通过（无测试间耦合） |
| `manage.py check` | 无问题 |
| `makemigrations --check --dry-run` | 无漂移 |
| `scripts/lint_redlines.py` | 通过（**但扫描集为空 —— 见 REV-18**） |
| `scripts/scan_secrets.py` | 通过 |
| `scripts/check_deps.py` | 通过（pyodbc 可选未装） |
| `npm run build` | 通过 |
| `npm test` (vitest) | 12 passed |
| `python monitor/tests_phase7.py` | 43/43 通过（**但无人自动执行 —— 见 REV-17**） |
| `python verify_phase7.py` | **崩溃**：`FileNotFoundError: '.env'` |
| `monitor/tests_v2_review.py`（本次新增） | **10 条全部失败（预期）** |
