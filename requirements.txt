# Django Web Framework
Django>=5.2.0

# 数据库驱动
pymysql>=1.1.0          # MySQL, Gbase8a, TDSQL
oracledb>=2.0.0         # Oracle
psycopg2-binary>=2.9.0  # PostgreSQL
pyodbc>=5.0.0           # 达梦数据库 (DM8 ODBC Driver)

# 任务调度
APScheduler>=3.10.0

# 密码加密
cryptography>=42.0.0

# 任务队列
celery>=5.3.0

# Redis（可选：生产环境设置 USE_REDIS_CACHE=True 时需要）
redis>=5.0.0

# Elasticsearch（指标数据存储）
elasticsearch>=8.0,<9.0

# Phase 8: LLM 直连 (OpenAI 兼容协议) + 结构化输出校验
requests>=2.31.0
jsonschema>=4.20.0

# 科学计算（cycle_detector / profile_engine / ml_anomaly_detection 依赖；BUG-016）
numpy>=1.26.0

# MongoDB (预留)
# pymongo>=4.0.0
