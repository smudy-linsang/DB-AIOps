# DB-AIOps v2.5 独立复审报告

**复审范围**：`a3daa90..99cfcd9`（29 个提交，80 个文件，+8874 / −3373）
**复审日期**：2026-08-17
**复审方式**：全量跑测 + 逐行阅读 + 编写复现用例实证 + 对 v2.0 整改逐条核验
**基线环境**：Python 3.11 / Django 5.2.17 / PostgreSQL 16（本地真实实例）

---

## 0. 结论摘要

两句话：**v2.0 的四个 P0 是真修好了，修得很扎实；但同一类问题在新增的 Copilot
工具层上原样复发了一遍。**

| | v2.0 复审 | v2.5 现状 |
|---|---|---|
| 数据范围越权 | REV-01（v2 接口族） | **复发** —— 这次在 Copilot 资产/记忆工具 |
| 编造数据当事实 | REV-04（阻塞拓扑接口） | **复发** —— 这次在 Copilot 的 ASH/执行计划/表空间工具 |
| 谎报执行成功 | REV-02 | ✅ 已彻底修复 |
| 硬编码处置目标 | REV-03 | ✅ 已修复（服务端从事故证据解析） |

本轮问题分级：

| 级别 | 数量 | 性质 |
|------|------|------|
| **P0** | 3 | 越权泄露 / 编造事实喂给 LLM / 凭据明文落库 |
| **P1** | 4 | 功能性缺陷与安全加固缺口 |
| **P2** | 5 | 设计不合理 |

本次新写 6 条复现用例，**当前 master 上 6 条全部失败**（见附录 A）。

跑测结论（项目自身用例全绿）：

| 检查项 | 结果 |
|--------|------|
| 后端全量 | 240 passed, 21 skipped |
| 随机顺序复跑 | OK（无测试间耦合） |
| 前端 build / vitest | 通过 / 12 passed |
| `manage.py check` / 迁移漂移 / 依赖完整性 | 通过 |

---

## 1. 先说 v2.0 整改的核验结论（这部分做得好）

我逐条核对了上一轮报告的 8 项，**不是看测试绿了就算数，而是读了修复后的代码**。
同时确认 `tests_v2_review.py` 里我原来的断言**没有被放宽**，只是摘掉了
`expectedFailure` 标记，并且额外补了 v1 事故处置的数据范围矩阵、SSRF、
幂等 bootstrap 等新用例。这是负责任的做法。

| 编号 | 原问题 | 核验结论 |
|------|--------|---------|
| REV-01 | v2 接口族无数据范围 | ✅ 新增 `_scoped_database` / `_scoped_incident`，越权与不存在**统一 404**、服务端日志区分二者 —— 与建议一致 |
| REV-02 | 自愈"执行"谎报成功 | ✅ 真实下发：走 `execute_run`，要求事故与实例匹配，接入 `decide_trigger` 审批/自治判定 |
| REV-03 | 前端写死 kill 目标 | ✅ `_resolve_params()` 从事故证据链解析 `blocker_id`，**明确不信任浏览器提交的用户名/SID**，无证据则拒绝执行 |
| REV-04 | 阻塞拓扑假数据 | ✅ 真连目标库采样 + 复用 v1 `_build_blocking_tree`，失败时返回 `degraded=True` 而非假数据 |
| REV-05 | `inc.severity` 必崩 | ✅ 已修 |
| REV-06 | 活跃状态值不存在 | ✅ 抽出 `ACTIVE_INCIDENT_STATUSES` 常量，覆盖全部处理中状态 |
| REV-07/08 | 体检编造默认值/单位混淆 | ✅ 已修 |
| REV-13 | `run_id` 可碰撞 | ✅ 改用 `uuid4().hex` |

**另外几项值得肯定的工程改进**（不在上轮报告要求内，是主动做的）：

- `monitor/process_lease.py`：带 fencing token 的分布式租约，`select_for_update`
  + TTL + 续租线程 + `assert_leader()`。常驻进程防重是真做了，不是加个标志位。
- `requirements.lock`：`uv pip compile --generate-hashes`，720 条 sha256，
  CI 改用 `pip install --require-hashes`。供应链这一步做得比多数项目到位。
- Webhook SSRF 加固：`_validated_webhook_url()` 强制 https + 官方域名白名单 +
  拒绝 URL 内嵌凭据 + 限定端口 + 禁止重定向 + 响应体 64 KiB 上限。**很完整。**
- `Django>=5.2.17,<5.3` 收敛上界（对应上轮 REV-20）。
- CI 增加 `ruff check --select F821,F601,F811`（抓未定义名等确定性错误）。

---

## 2. P0 —— 必须在上线前解决

### R25-01【安全】Copilot 工具层无视数据范围，泄露全部纳管实例

**定位** `monitor/copilot.py:45`（`get_global_managed_inventory`）、
`:209`（`recall_memory_palace`）、`:156`（`get_tablespace_and_capacity_status`）

`CopilotChatView` 对**显式传入**的 `config_id` 做了范围校验（`_get_config`），
这部分是对的。但 `run_copilot_chat()` **根本没有接收 user 参数**，其内部工具直接全表查：

```python
def get_global_managed_inventory():
    configs = DatabaseConfig.objects.filter(is_active=True)   # 没有任何范围过滤
    ...
    summary.append({'name': c.name, 'host': c.host, 'port': c.port, ...})
```

触发词包含 `'所有库' '纳管' '资产' '清单' '多少库' '拓扑' 'inventory' 'database'`
—— 注意 `'database'` 也在列表里，随便一句英文提问就能触发。

**实证**：数据范围仅限 `copilot-mine` 的用户提问"我们一共纳管了哪些库？"，
返回体里出现了范围外实例的完整信息：

```
| 2 | **核心交易库-生产** | `oracle` | 10.88.88.88:1521 | UNKNOWN | 0 | LNone |
```

`recall_memory_palace()` 同样是 `CopilotMemory.objects.all()`
（且因 R25-04 的变量问题，`config` 恒为 None）—— 会把**其他实例的历史排障过程、
DBA 偏好、事故推导内容**一并召回给无权用户。

**这是 BUG-103 → REV-01 → R25-01 的第三次复发。** 前两次都发生在"新写一批接口时
忘了套范围"，这次是"新写一层工具时，连传 user 的通道都没留"。

**建议** `run_copilot_chat(query, config_id, history, *, user)` 显式接收调用者；
所有工具函数强制接收 `allowed_ids` 并过滤。更根本的办法是把数据范围下沉到
QuerySet 层（如 `DatabaseConfig.objects.visible_to(user)` 管理器），
让"忘记过滤"从可能变成不可能 —— 否则第四次复发只是时间问题。

---

### R25-02【正确性】Copilot 工具对零数据实例编造具体事实，并作为"观测结果"喂给 LLM

**定位** `monitor/copilot.py:116`（ASH）、`:133`（执行计划）、`:176-189`（表空间兜底）

三个工具在拿不到真实数据时不是降级，而是**返回编造的具体事实**。
`get_realtime_ash()` 甚至完全不使用 `config`（除了挑一个 wait_event 字符串）。

**实证**（一个刚纳管、从未采集过任何数据的 Oracle 实例）：

```
get_realtime_ash():
   sql_id       = 8a7fbc6d
   sql_text     = UPDATE trade_order SET status = 2 WHERE batch_id = 90218
   阻塞会话数   = 18
   top_sessions = {'session_id': '1845', 'user': 'app_trade_user', ...}

get_tablespace_and_capacity_status():
   SYSTEM  used_pct=90.3
   SYSAUX  used_pct=69.3
   USERS   used_pct=86.3
   高水位表空间数 = 2

explain_sql():
   UPDATE trade_order                      cost=18450.2
    -> Filter: (batch_id = 90218)          cost=18450.2
        -> Table scan on trade_order       cost=18400.0
```

**这些正是 v2.0 REV-04 里那批假数据**（会话 1845 / app_trade_user /
`trade_order` UPDATE）—— 接口层修好了，同一批固定值迁移到了工具层。

**为什么比 v2.0 那次更严重**

1. **它有了受众。** 这些结果作为 `tool_results` 注入 LLM 提示词，模型会把它们
   当作既定观测事实，组织成权威口径的诊断结论，再由 `_build_action_cards()`
   渲染成可点击的动作卡片。编造 → 推理 → 建议 → 按钮，一条龙。
2. **它编造的是具名对象。** 不是"性能 100 分"这种笼统评分，而是
   "SYSTEM 表空间 90.3%""2 个高水位表空间"——足以触发一次针对**不存在的对象**
   的容量扩容流程。
3. **零数据的新实例最容易命中。** 刚纳管、还没采集的库，恰恰是最需要如实回答
   "暂无数据"的时候。

**建议** 三个工具一律改为：有真实数据才返回，没有就返回
`{'available': False, 'reason': ...}`，并让 system prompt 明确"无数据时必须
如实告知，不得推测"。执行计划应接 `plan_capture.py`，索引建议接 `index_advisor.py`
—— 这两个真实模块项目里都有。

---

### R25-03【安全】大模型 API Key 明文落库，而字段名写着"加密存储"

**定位** `monitor/models.py:2287`、`monitor/api_views_phase8.py:702, 738`、
`monitor/llm/router.py:93`

```python
api_key = models.CharField(max_length=255, ..., verbose_name="加密存储的 API Key")
```

全链路没有任何加解密：写入 `cred.api_key = data['api_key'].strip()`，
读取 `api_key=cred.api_key`。

**实证**（同一个库、同一次事务内对比）：

```
LLM api_key 落库值   : sk-live-SUPERSECRET-abcdef
DB  password 落库值  : enc:nA6F4UVaK3pokQbi2NJJRMSg
```

**项目自己就有正确做法**：`monitor/crypto.py` 的 `encrypt_password/decrypt_password`
（`enc:` 前缀，AES），数据库密码一直是加密存的。新模块没有复用。

**影响** 大模型 API Key 是**可直接计费**的凭据。数据库备份、只读从库、
运维导出、日志误采都会造成直接经济损失。而字段 `verbose_name` 声称"加密存储"，
会让审计者误以为已加密 —— **错误的安全声明比没有声明更危险**。

**建议** 复用 `crypto.py`（写入 `encrypt_password`，路由取用时 `decrypt_password`）；
为存量数据写一次性迁移；顺手把 `verbose_name` 改成与事实一致。
接口返回已做掩码（`sk-x****xxxx`），这一点是对的，保持。

---

## 3. P1

### R25-04【功能】`config` 在赋值前被两个工具引用，用户选中的目标库被忽略

**定位** `monitor/copilot.py:412-448`

```python
config = None                                    # 412
...
tool_results['alerts_and_baselines'] = get_alerts_and_baseline_status(config=config)   # 423 → None
tool_results['tablespace_and_capacity'] = get_tablespace_and_capacity_status(config=config)  # 432 → None
recalled_memories = recall_memory_palace(query, config=config)                        # 426 → None
if config_id:
    config = DatabaseConfig.objects.filter(id=config_id).first()                      # 435 才赋值
```

用户在界面上明明选了目标库，告警基线、表空间、记忆召回这三个工具却按
**全局**跑（`config=None` 时遍历所有 `is_active` 实例）。
既是功能错误（答非所问），也放大了 R25-01 的泄露面。

Python 不会报错，因为 `config` 已在 412 行绑定为 `None` —— 属于典型的
"能跑但语义错"，ruff 的 F821 抓不到。

**建议** 把 `config` 解析提到所有工具触发之前。

---

### R25-05【安全】LLM `base_url` / `proxy_url` 无任何校验（与同版本 webhook 的严格加固形成反差）

**定位** `monitor/models.py:2286,2289`、`monitor/llm/providers.py:97-105,188-194`

`base_url` 与 `proxy_url` 由管理员自由填写，服务端据此发起出站请求
（**并携带该凭据的 API Key**），且 `proxy_url` 会直接进 `requests` 的 `proxies`。
没有 scheme 限制、没有私网/元数据地址拦截、没有域名白名单。

同一个版本里，webhook 通道做了非常完整的 SSRF 防护（见 §1）。
**同样是"服务端按配置发出站请求"，两条路径的安全标准不一致。**

**准确说明严重度**：所有写入类 LLM 端点都有 `is_superuser` 内层校验（我逐个核过），
因此这不是权限提升，属于"管理员误配置 / 管理员账号被盗后的横向移动"面
（例：`base_url` 指向 `http://169.254.169.254/…` 拿云元数据，
或用 `proxy_url` 把全部模型流量导向可监听的代理）。

**建议** 至少强制 https、拒绝私网与链路本地地址、拒绝 URL 内嵌凭据；
`proxy_url` 建议改为部署期配置（环境变量）而非可由 Web 修改。

---

### R25-06【工程】红线门禁进了 CI，但结构性无法生效（实证）

**定位** `.github/workflows/ci.yml:37-40` + `scripts/lint_redlines.py:45`（**文件未改动**）

上轮 REV-18 提了两点：①把 `lint_redlines.py` 接入 CI；②CI 下扫描范围要从
"工作区 diff"改成"提交范围 diff"。**只做了 ①。**

`_changed_files()` 仍然基于 `git diff HEAD`。CI 是干净检出 → 扫描集为空 → 恒过。

**实证**（在 CI 同等条件下：干净工作区）：

```
工作区是否干净: 0 个变更（等同 CI 检出状态）
红线约束扫描通过
红线扫描退出码=0
```

—— 而此时 `monitor/degrade.py` 里就躺着一处我故意植入并已提交的
`try/except Exception: pass`。**门禁看着上线了，实际一条也拦不住。**

**需要说明的是**：我用 AST 扫了 v2.5 全部改动，**新增行里的红线违规是 0 处**
（不是被漏掉，是确实没有）。所以这是一个**潜在**漏洞而非正在发生的问题，
但它会让所有人以为红线已经有人守了。

**建议** CI 模式下改用 `git diff origin/master...HEAD`（PR）或 `HEAD~1..HEAD`（push）；
或干脆在 CI 里全量扫描新增文件。

---

### R25-07【性能/正确性】记忆召回：读路径写库 + 检索方式失真

**定位** `monitor/copilot.py:209-240`

```python
for mem in mem_qs[:20]:            # 先按 (-importance, -updated_at) 取前 20
    ...
    if any(k.lower() in searchable for k in keywords):   # 再在 Python 里子串过滤
        mem.access_count += 1
        mem.save(update_fields=[...])                    # 读路径上逐条写库
```

两个问题：

1. **先截断再过滤**：相关度排名第 21 的记忆永远召不回来。这不是"记忆宫殿检索"，
   是"取重要度前 20 再做子串匹配"。随着记忆增长会越来越查不准。
2. **每轮对话最多 20 次 UPDATE**，且发生在 GET 语义的问答路径上。

**建议** 过滤下推到数据库（`Q(title__icontains=...) | Q(content__icontains=...)`）
再截断；访问计数改为异步或批量 `F()` 更新。

---

## 4. P2 —— 设计不合理

### R25-08 权限装饰器与实际管控不一致，RBAC 被绕开

11 个 LLM 端点全部用 `require_permission(Perm.ALERT_CONFIG_VIEW)`——
一个 **VIEW** 权限挂在创建/修改/删除凭据的写端点上；真正的管控是每个 handler
内层硬编码的 `if not request.user.is_superuser`。

后果：① 看装饰器无法判断端点的真实权限要求，审计困难；
② 权限体系里已有的 `Perm.ALERT_CONFIG_MANAGE` 闲置未用；
③ 授权粒度退化为"超管 / 非超管"二元，RBAC 形同虚设。

**建议** 写端点改用 `ALERT_CONFIG_MANAGE`（或新增 `LLM_CREDENTIAL_MANAGE`），
删除内层 `is_superuser` 硬编码。

### R25-09 `LlmConfigView.put` 运行时改 settings 并回写 .env

`settings.LLM_API_KEY = api_key` 是**进程内**赋值：多 worker 部署下只有处理该请求的
worker 生效，其余 worker 仍用旧值，直到重启 —— 行为随机且难排查。
同时把明文 Key 写进 `BASE_DIR/.env`（Web 请求改部署期文件）。

**建议** 统一走 `LLMProviderCredential` 表 + 缓存失效通知，不要改 `settings`。

### R25-10 "多工具 ReAct" 名不副实

提交信息与文档称 `multi-tool ReAct`，实现是 `run_copilot_chat` 里的中文关键词
`if any(k in q for k in [...])` 硬编码分支，工具由 Python 预先选好、结果一次性
塞进提示词，没有 LLM 决策、没有多轮观察-行动循环。

功能上可以接受（关键词路由是常见的低成本方案），但**命名应与实现一致**，
否则后续维护者会按 ReAct 的假设去改它。

### R25-11 Copilot 里残留的硬编码处置目标

`copilot.py:443,446` 仍写着示例 SQL 与 `{'username': 'app_trade_user', 'session_id': '1845'}`。

**核实结论**：这条调用传的是 `incident=None`，会被 v2.5 新增的 `_resolve_params()`
以"终止阻塞会话必须关联有效事故"直接拒绝 —— **当前不可被利用**。
但它是一段会产生误导的死代码，且一旦将来有人给它补上 incident 就会立刻变成隐患。
建议删除或改为从真实上下文取值。

### R25-12 `init_default_playbooks()` 仍在读路径上

上轮 REV-12 提到的"每次预演都 `update_or_create` 覆盖模板"依然存在于
`evaluate_dryrun()` 第一行。虽然 v2.5 给 `Playbook`（统一执行引擎那张表）做了
幂等 bootstrap 并有测试保护 DBA 自定义，但 `PlaybookTemplate` 这张表仍会被覆盖。

---

## 5. 一个整体判断

v2.5 的整改质量是高的 —— 上一轮四个 P0 全部真修，而且修在了根因上
（服务端解析处置目标、接入既有审批引擎、复用 v1 阻塞树），不是应付测试。
分布式租约和供应链锁定这两项甚至超出了要求。

**但同一类错误换了个楼层又出现了一次。**

v2.0 的教训是"新写的接口忘了套数据范围、拿假数据充真实结果"；
v2.5 把接口层修好了，然后在新盖的 Copilot 工具层重犯了同样两条。
差别只在于：这次假数据会被送进大模型，由它用专业口吻复述给运维人员。

这说明问题不在某一次疏忽，而在于**没有把约束固化成机制**：

- 数据范围靠"每个视图记得调 helper"→ 换个楼层就忘（已复发三次）。
  应下沉到 QuerySet 管理器，让忘记过滤在编译期/调用期就不可能。
- "不许拿样例数据冒充真实观测"目前只是口头共识，没有任何检查。
  可以做一条很便宜的静态检查：工具函数返回值里出现固定业务字面量
  （如 `trade_order` / `1845` / `app_trade_user`）即告警。
- 红线扫描器本该是这类机制，但它在 CI 里是空转的（R25-06）。

**建议的下一步顺序**：先修 R25-01/02/03（越权、编造、明文凭据），
再修 R25-06 让门禁真正生效 —— 否则下一轮还会有 R30-01。

---

## 6. 修复优先级

| 顺序 | 条目 | 理由 |
|------|------|------|
| 1 | R25-03 凭据明文 | 一次性迁移即可，风险与成本比最高 |
| 2 | R25-01 Copilot 数据范围 | 越权泄露，且建议顺带下沉到管理器层根治 |
| 3 | R25-02 工具编造事实 | 影响诊断决策；改成"无数据即如实说"成本很低 |
| 4 | R25-04 config 未赋值 | 一行位置调整 |
| 5 | R25-06 红线门禁失效 | 让机制真正生效，防止再复发 |
| 6 | R25-05/07/08 | 安全加固与设计收敛 |

**验收**：`docs/review/v2.5/tests_v25_review.py`（6 条）当前全部失败，修复后应转绿。

> **关于这些用例放在哪里，需要说明取舍。**
>
> 上一轮我用 `@unittest.expectedFailure` 让复现用例能进 `monitor/` 而不挂红。
> 但 v2.5 已在 `tests_v2_review.py` 头部立下规矩 ——
> **"新增缺陷不得以 expected failure 方式换取绿灯"**。这条规矩是对的，本轮不再用那招。
>
> 于是它们暂放在 `docs/review/v2.5/`（Django 不会自动发现），**代价是没有任何
> 自动化会跑它们** —— 这恰恰是本报告 REV-17 批评过的
> "断言存在但没人执行"的状态。所以这是**临时位置，不是归宿**：
> 修好一项就把对应用例移回 `monitor/tests_v25_review.py`，它随即转正为回归防线；
> 全部修完时该目录应为空。用法见同目录 `README.md`。

---

## 附录 A：复现用例执行结果

```
$ DJANGO_SETTINGS_MODULE=dbmonitor.settings_test_unit \
  python manage.py test monitor.tests_v25_review -v 2

FAIL: test_inventory_must_not_leak_out_of_scope_databases
      → '10.88.88.88' 出现在回答中（范围外实例 host 泄露）
FAIL: test_ash_tool_must_not_invent_sessions
      → 零数据实例返回 'UPDATE trade_order ... batch_id = 90218'、18 个阻塞会话
FAIL: test_tablespace_tool_must_not_invent_high_watermark
      → 零数据实例报告 2 个高水位表空间（编造：SYSTEM/SYSAUX/USERS/UNDOTBS1/TEMP）
FAIL: test_explain_tool_must_not_invent_execution_plan
      → 未连接目标库却返回 cost=18450.2 的执行计划
FAIL: test_tablespace_tool_respects_selected_database
      → config 赋值前即被工具引用，选中的库被忽略
FAIL: test_api_key_must_not_be_stored_in_plaintext
      → 落库值 == 'sk-live-SUPERSECRET-abcdef'

Ran 6 tests — FAILED (failures=6)
```

## 附录 B：本次复审执行的验证

| 验证 | 结果 |
|------|------|
| `manage.py test monitor` | 240 passed / 21 skipped |
| `manage.py test monitor --shuffle` | OK（排除本次新增复审文件后） |
| `manage.py check` / 迁移漂移 / 依赖完整性 | 通过 |
| `scripts/lint_redlines.py` | 通过（**干净树下扫描集为空 —— 见 R25-06**） |
| `scripts/scan_secrets.py` | 通过（标记已由 `noqa: secret` 全量迁移至 `secret-scan: allow`） |
| `npm run build` / `npm test` | 通过 / 12 passed |
| v2.0 复审断言是否被放宽 | **否** —— 仅摘除 expectedFailure，另有新增用例 |
| v2.5 新增行的红线违规（AST 全扫） | **0 处** |
| LLM 凭据落库形态（真实 SQL 查询） | 明文 |
| Copilot 工具对零数据实例的返回 | 编造具名事实 |
| 红线门禁在 CI 同等条件下的有效性 | 植入违规后仍退出码 0 |
