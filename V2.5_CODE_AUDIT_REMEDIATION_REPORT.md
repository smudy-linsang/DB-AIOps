# DB-AIOps v2.5 全量代码审计与整改报告

> 审计日期：2026-08-16  
> 审计基线：`master@6320e95`  
> 审计范围：仓库内 483 个受版本控制文件；约 77,500 行代码，其中 200 个 Python 文件约 60,843 行  
> 报告性质：上线前差距评估、缺陷台账和整改验收依据

## 1. 执行结论

当前版本可以完成开发环境下的基本单元测试和前端构建，但**不满足银行生产上线条件**。主要原因不是页面功能缺失，而是以下四类底线问题同时存在：

1. 生产编排把 `DJANGO_ENV` 配成 `production`，代码只识别 `prod`，可能以 `DEBUG=True`、开发密钥和进程内缓存启动。
2. V2 自愈接口存在越权读取、伪造阻塞数据和“未连接目标库却记为执行成功”等真实性与审计问题。
3. 诊断流水线会把 4NF 关系属性 `plans` 当作模型字段保存，SIT 中稳定抛错；时序游标异常路径会二次 `yield`，掩盖原始 SQL 错误。
4. 依赖、安全门禁、覆盖率、守护进程和高可用治理不足，无法证明系统在故障、并发和攻击场景下仍然可信。

因此本轮整改采用“先止血、再收敛、后升级”的顺序：先消除 P0/P1 正确性和安全缺陷，再补齐生产配置、测试门禁和可观测性，最后实施 v2.5 的银行级治理能力。

## 2. 审计方法与边界

### 2.1 已执行工作

- 按文件清单读取全部受控源码、配置、迁移、脚本、测试和文档，并对 Python AST 做结构化扫描。
- 执行全仓 Ruff、Bandit、npm audit、pip-audit、前端 Biome、Django 测试、Vitest、Vite build。
- 对 Django URL、鉴权、数据范围、目标库连接、时序连接、异常降级、配置中心、LLM 输出校验逐模块追踪。
- 检查开发容器、操作系统进程、健康检查、生产 Compose、Dockerfile 和 GitHub Actions。
- 执行 MySQL/PostgreSQL/Oracle 实库方言测试及 Phase 5/6/7/8 验证脚本。

### 2.2 口径说明

“全量检查”指对当前基线及可用测试环境进行穷举式静态扫描和风险驱动动态验证；它不等于数学意义上证明未来不存在缺陷。未获得真实达梦、GBase 商业授权环境、银行统一认证、真实短信网关和灾备机房，因此这些部分以契约测试、降级验证和设计验收为主，必须在投产前环境补做实证。

## 3. 风险分级

| 级别 | 定义 | 上线决策 |
|---|---|---|
| P0 | 可导致越权、错误执行、失真审计、生产不安全启动或核心链路不可用 | 未关闭不得上线 |
| P1 | 用户可见错误、故障时不可恢复、监控结论不可信 | 原则上本版本关闭 |
| P2 | 可维护性、性能、测试或运维治理明显不足 | 纳入 v2.5 并给出验收证据 |
| P3 | 规范、体验和长期演进问题 | 排期治理，不得继续扩大债务 |

## 4. 缺陷总表

| ID | 级别 | 领域 | 问题摘要 | 基线状态 | v2.5 验收 |
|---|---|---|---|---|---|
| AUD-001 | P0 | 生产配置 | `production` 未命中 `prod`，生产可能以开发配置启动 | 未关闭 | 非法环境值拒绝启动，生产强制安全项 |
| AUD-002 | P0 | 密钥/密码 | Compose 和 settings 存在默认口令/开发密钥回退 | 未关闭 | 生产缺密钥即失败，示例值不进入正式编排 |
| AUD-003 | P0 | 数据隔离 | V2 WarRoom/阻塞/剧本接口未统一校验实例数据范围 | 7 个预期失败测试证明 | 全部转为通过并补反向用例 |
| AUD-004 | P0 | 自愈真实性 | V2 剧本不连接目标库却写成功审计 | 未关闭 | 复用真实执行引擎；失败、拒绝、审批完整留痕 |
| AUD-005 | P0 | 阻塞分析 | V2 阻塞图返回硬编码会话、用户、SQL、IP | 未关闭 | 只返回目标库实时或明确无数据 |
| AUD-006 | P0 | 诊断流水线 | `save(update_fields=...)` 包含非字段 `plans` | SIT 稳定失败 | Phase 6/8 全通过 |
| AUD-007 | P0 | 时序连接 | `cursor()` 捕获消费端异常后二次 yield | SIT 稳定失败 | 保留原始异常且连接正确归还/淘汰 |
| AUD-008 | P0 | TSDB 漂移 | 代码读取 `wait_secs/trx_age_secs`，运行库缺列 | 实库失败 | 启动前 schema 检查，迁移幂等，查询通过 |
| AUD-009 | P0 | 审计一致性 | RCA GET 触发写；先删后插无事务 | 未关闭 | 读写分离、原子替换、幂等 request_id |
| AUD-010 | P1 | RCA 可信度 | Oracle 等证据、资源节点和置信度硬编码 | 未关闭 | 证据来源可追溯，无证据不造结论 |
| AUD-011 | P1 | 指标语义 | `threads_connected` 绝对值当百分比 | 预期失败 | 使用连接上限计算或标记未知 |
| AUD-012 | P1 | 状态机 | 活跃事故过滤使用不存在的 `investigating` | 预期失败 | 与 Incident 状态枚举一致 |
| AUD-013 | P1 | 目标保护 | 前端硬编码用户和 session id，保护逻辑依赖客户端输入 | 未关闭 | 服务端从实库解析主体，前端只提交选择项 |
| AUD-014 | P1 | 模板治理 | 每次请求 `update_or_create` 覆盖 DBA 自定义剧本 | 未关闭 | 初始化只创建；变更走版本、审批和发布 |
| AUD-015 | P1 | ID 唯一性 | 秒级 `run_id` 并发碰撞 | 未关闭 | UUID/数据库唯一约束 |
| AUD-016 | P1 | 健康检查 | `/api/v1/health` 未覆盖 sentinel/pipeline/TSDB 等关键组件 | 运行态误报健康 | readiness/liveness 分离并正确返回状态码 |
| AUD-017 | P1 | 进程治理 | 同机出现双 runserver、双 collector，无 leader election | 实测存在 | 数据库/Redis 租约与唯一实例告警 |
| AUD-018 | P1 | 进程缺失 | 开发运行态无 sentinel/pipeline，生产 Compose 也未编排 | 实测存在 | 角色齐全、心跳与重启策略齐全 |
| AUD-019 | P1 | Timescale | Compose 创建库与应用默认库名不一致 | 未关闭 | 新环境一键启动且 schema 自检通过 |
| AUD-020 | P1 | 缓存一致性 | Compose 配 REDIS_HOST，settings 只读 REDIS_URL | 未关闭 | 单一配置契约，生产共享缓存可用 |
| AUD-021 | P1 | 依赖安全 | Django/cryptography、Axios/ECharts/Router 有已知漏洞 | 审计失败 | 固定受支持版本且 audit 无 high/critical |
| AUD-022 | P1 | 版本漂移 | Django 无上界，Py3.11 CI 与本地 Py3.12 解析不同大版本 | 未关闭 | 5.2 LTS 兼容窗口与锁文件一致 |
| AUD-023 | P1 | Webhook | 通知 URL 允许任意 scheme/目标，存在 SSRF 风险 | 未关闭 | HTTPS、域名策略、私网阻断、超时/大小限制 |
| AUD-024 | P1 | CI 门禁 | pip-audit 使用 `|| true`，无 npm audit，红线扫描无效 | 未关闭 | 安全与红线检查失败即红 |
| AUD-025 | P1 | 测试隔离 | 测试 settings 未关闭 LLM/Agent，测试可能访问外网 | 已实测 15s 调用 | 默认完全离线，外部测试显式标签 |
| AUD-026 | P1 | 前端 API | `apiV2` 直接 axios，绕过超时、401、重试、错误归一化 | 未关闭 | 所有请求统一实例 |
| AUD-027 | P2 | 覆盖率 | 核心 Python 总覆盖率约 26%，多条关键链路 0% | 不达标 | 核心安全/执行模块 >=80%，增量 >=85% |
| AUD-028 | P2 | 静态质量 | Ruff 2,643 项；宽泛捕获 772、try/pass 182 | 不达标 | 新增代码零违规，高风险旧债清零 |
| AUD-029 | P2 | 未定义符号 | 8 处 F821，包括管理命令 logger/timezone | 未关闭 | F821=0 并有路径测试 |
| AUD-030 | P2 | 重复定义 | PG 采集器重复字典键；前端 API 重复 `create` 键 | 未关闭 | 重复定义扫描为零 |
| AUD-031 | P2 | 复杂度 | 58 个超长/高复杂函数，采集器单函数 724–1561 行 | 未关闭 | 方言查询、转换、降级拆层并加契约测试 |
| AUD-032 | P2 | 异常治理 | 835 处宽泛异常，208 处近似静默 | 未关闭 | 核心链路结构化留痕，禁止完全静默 |
| AUD-033 | P2 | SQL 安全 | 68 处动态 execute 需逐项证明标识符白名单 | 待治理 | 值参数化；动态标识符显式白名单 |
| AUD-034 | P2 | 容器化 | 生产前端运行 Vite dev server，Node 18 且启动时 npm install | 未关闭 | 多阶段静态构建、固定依赖、非 root |
| AUD-035 | P2 | 网络暴露 | Redis/ES/数据库端口外露，ES 关闭安全 | 未关闭 | 内网网络、最小暴露、认证/TLS 策略 |
| AUD-036 | P2 | 镜像供应链 | 使用浮动镜像标签，无摘要/SBOM/镜像扫描门禁 | 未关闭 | 固定版本/摘要、生成 SBOM、阻断高危 |
| AUD-037 | P2 | Auth | localStorage token 可被 XSS 窃取；cookie fallback 与 csrf_exempt 冲突 | 未关闭 | 明确单一认证模式；Cookie 模式强制 CSRF |
| AUD-038 | P2 | 健康语义 | degraded 仍 200，旧健康接口版本写死 0.1.0 | 未关闭 | liveness 仅进程，readiness 对关键依赖非 2xx |
| AUD-039 | P2 | 配置治理 | 新旧环境变量命名分散，部分配置未进入 SPECS | 未关闭 | 类型化、可审计、启动校验、敏感项脱敏 |
| AUD-040 | P2 | 前端质量 | Biome 122 errors/186 warnings；Hook 依赖、index key 等 | 未关闭 | 行为类错误清零、格式基线收敛 |
| AUD-041 | P2 | 包体积 | ECharts 676KB、AntD 1.177MB，首屏包偏大 | 未关闭 | 路由/图表按需加载与预算门禁 |
| AUD-042 | P2 | 数据保留 | 连续聚合与原始时序缺少容量/保留验收 | 风险存在 | 分层保留、压缩、容量水位和演练 |
| AUD-043 | P2 | DB 先决条件 | 被监控 PG 未启用 pg_stat_statements | SIT 失败 | 纳管预检给出可执行提示并降级 |
| AUD-044 | P2 | 兼容性 | `pyodbc` 缺 unixODBC 时导入失败 | 环境可复现 | 可选驱动能力探测，不影响其他方言 |
| AUD-045 | P3 | UI 兼容 | Ant Card API 弃用、Router future flag 警告 | 未关闭 | 升级并消除运行警告 |
| AUD-046 | P3 | 文档版本 | README、健康接口、模块内版本号不一致 | 未关闭 | 单一版本源 `2.5.0` |

## 5. P0/P1 详细分析

### 5.1 生产环境识别和密钥策略（AUD-001/002）

`docker-compose.yml` 传入 `DJANGO_ENV=production`，`dbmonitor/settings.py` 只在值为 `prod` 时启用生产分支。这不是文字差异，而会改变 DEBUG、SECRET_KEY 回退、缓存和安全头的实际行为。多 Gunicorn worker 使用 LocMemCache 时，登录 token 和限流状态也无法跨 worker 一致。

整改要求：

- 环境名归一为 `development/test/production`，兼容旧别名但对未知值直接报错。
- production 缺 `DJANGO_SECRET_KEY`、数据库口令、`DB_MONITOR_SECRET_KEY` 时拒绝启动。
- production 强制 `DEBUG=False`、共享缓存、安全 Cookie、HTTPS 代理头和受控 host/origin。
- 示例编排使用 `${VAR:?message}`，不再提供可误用的正式默认密码。

### 5.2 V2 数据范围与自愈真实性（AUD-003/004/005/013/014/015）

旧版执行链已有审批、自治等级、目标连接、预检、步骤执行和验证模型；V2 又新建了一套 `PlaybookTemplate/PlaybookRunRecord`，却只模拟成功。两套执行面造成安全策略分叉，且审计记录不能证明目标库发生过什么。

整改要求：

- V2 API 先取得用户可管理的 `Database` queryset，再解析 incident/database/playbook。
- 阻塞图复用真实采样能力，不得用固定 SID、SQL、IP 填充“演示数据”。
- `KILL_ROOT_BLOCKER` 兼容名映射到真实 `PB-LOCK-KILL-BLOCKER`；执行委托既有引擎。
- 预演只能返回基于实时目标解析出的动作；执行必须带幂等键、审批证据和服务端解析的受保护主体。
- 初始化模板只做 `get_or_create`；发布后不可原位覆盖，采用版本化。

### 5.3 诊断与时序基础设施（AUD-006/007/008/009/010）

`Incident.plans` 是规范化关系的兼容属性，不是数据库列。将其加入 `update_fields` 会使诊断在保存阶段失败。时序游标把 `yield` 包在捕获块内，调用方 SQL 异常会被当作“建游标失败”处理，并再次 yield，最终出现 `generator didn't stop after throw()`。

整改要求：

- 诊断仅保存实体字段，计划通过关系模型和专用同步函数持久化。
- 游标管理器只捕获连接/游标获取阶段；消费端异常原样抛出，连接按健康状态归还。
- 时序 schema 变更使用幂等迁移；进程启动做版本检查，缺列时 readiness 失败而不是运行到查询时才报错。
- RCA 写入改为 POST/异步命令；替换图谱须在 `transaction.atomic()` 内完成。
- 每条 RCA 结论保存证据类型、采样时间、目标实例、查询/指标标识和算法版本；缺证据时输出“证据不足”。

### 5.4 依赖与供应链（AUD-021/022/034/036）

实测 pip-audit 报告 Django 6.0.5 与 cryptography 48.0.0 共 12 项应用依赖漏洞；npm audit 报告 2 high、4 moderate。`Django>=5.2.0` 又导致 Python 3.11 CI 和 Python 3.12 本机解析到不同大版本。

整改要求：

- 服务端统一在 Django 5.2 LTS 的受支持补丁窗口，约束 `>=5.2.16,<5.3`，锁文件与 CI 一致。
- cryptography 升至修复版本；前端升级 Axios、ECharts、React Router 及传递依赖。
- 生成锁文件、SBOM 和依赖审计证据；critical/high 不允许 `|| true`。
- 前端产物在构建阶段生成，由 Nginx/等效静态服务器交付，运行容器不安装依赖。

### 5.5 进程、高可用和健康检查（AUD-016/017/018/019/020/038）

现场进程检查发现两个 runserver 和两个 collector，同时 sentinel/pipeline 缺失；旧健康接口仍返回 healthy。这会导致重复采集、重复告警、诊断不运行，却无法从探针发现。

整改要求：

- 采集、告警、诊断调度均使用带 fencing token 的租约选主；重复实例可热备但只有 leader 执行业务。
- 每个角色按固定周期写 `ComponentHeartbeat`，包含版本、实例 ID、leader 状态、最后成功批次和错误摘要。
- `/livez` 只证明进程可响应；`/readyz` 校验 ORM、Timescale schema、Redis、角色心跳，并对关键失败返回 503。
- Compose/Kubernetes 明确编排 web、collector、sentinel、pipeline、worker；Timescale 库名和 schema 初始化一致。

## 6. 静态质量问题治理方案

### 6.1 未定义符号和重复定义

首批必须清理以下确定性缺陷：

- `monitor/api_views.py` 关键字过滤使用未导入的 `models`。
- `monitor/management/commands/start_monitor.py` 使用未定义 `logger`、`timezone`。
- `monitor/report_engine.py` 使用未导入 `timezone`。
- `monitor/views_enhanced.py` 使用未导入 `AutoRemediationEngine`。
- `monitor/health_engine.py` 类型引用未正确声明。
- PostgreSQL 采集器字典重复键；`frontend/src/services/api.js` 对象重复 `create` 键。

### 6.2 异常处理

宽泛异常并非一律错误，但当前数量和静默比例已使故障不可诊断。治理规则：

1. 边界层可捕获 `Exception`，但必须使用 `monitor.degrade` 的分级留痕并保留 cause。
2. 领域层只捕获可恢复的具体异常；未知异常向上抛。
3. 禁止 `except: pass`、空 fallback 和把真实数据错误替换为“正常/满分”。
4. 降级结果必须包含 `degraded=true`、原因码和数据时效，不与成功结果混同。

### 6.3 动态 SQL

68 处动态 SQL 按以下规则逐项收敛：

- 值全部由驱动参数绑定。
- 表名、列名、排序方向等无法参数化的标识符必须来自代码内白名单并用方言安全引用。
- 禁止将 URL 参数、LLM 输出或剧本参数直接拼入 SQL。
- 执行前记录 SQL 指纹而非完整敏感 SQL；审计中对字面量脱敏。

### 6.4 巨型采集函数

采集器按“能力探测—查询模板—结果映射—单位归一—降级”拆分。公共指标采用标准 DTO，方言模块只负责适配。每种数据库至少包含：版本矩阵、权限不足、扩展缺失、超时、空结果和类型边界契约测试。

## 7. 整改批次和验收门槛

| 批次 | 范围 | 退出条件 |
|---|---|---|
| R1 止血 | AUD-003～015、029～030 | 7 个 expectedFailure 转通过；Phase 6/8 通过；F821=0 |
| R2 安全基线 | AUD-001～002、021～026、037、039 | 生产配置 fail-fast；audit 无高危；接口越权反测通过 |
| R3 运行治理 | AUD-016～020、034～036、038 | 新环境一键启动；角色齐全；readiness 可检出故障 |
| R4 质量收敛 | AUD-027～033、040～045 | 核心覆盖率和增量门槛达标；关键 lint 清零 |
| R5 v2.5 能力 | 证据化 RCA、执行面统一、容量/SLO | UAT 场景通过，审计证据可复核 |

每个批次必须执行 `scripts/validate.sh unit --json`；后端变更执行 backend，前端变更执行 frontend，两侧变更执行 all。涉及数据库方言时补 MySQL/PostgreSQL/Oracle 实库测试；提交后检查 7 项 CI。

## 8. 上线放行清单

- [ ] P0 全部关闭，P1 无未接受风险。
- [ ] 生产配置在缺密钥、错环境名、错 schema 时均拒绝启动。
- [ ] 任何用户不能读取或操作授权范围外实例。
- [ ] 每次自治执行都能回答：谁批准、对哪个实例、执行了什么、结果如何、如何回滚。
- [ ] 无数据和采集失败不会被表达为“健康”或满分。
- [ ] Django、前端生产依赖无 critical/high 已知漏洞。
- [ ] 核心执行链路覆盖率达到门槛，性能和故障注入结果达标。
- [ ] 备份恢复、灾备切换、监控平台自身降级均完成演练并归档证据。

