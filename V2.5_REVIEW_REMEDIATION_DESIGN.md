# DB-AIOps v2.5 独立复审整改设计

## 1. 目标与范围

本设计对应 `V2.5_INDEPENDENT_REVIEW.md` 的 R25-01 至 R25-12。整改目标是：

1. Copilot 的资产、告警、记忆、容量、ASH、计划工具只能读取调用者有权访问的实例；
2. 没有观测数据时返回结构化不可用状态，禁止用样例值填充事实；
3. LLM API Key 采用与数据库密码相同的 AES-256-GCM 信封格式落库；
4. LLM 出站端点在保存和调用两层校验，Web 不再配置代理；
5. LLM 写操作由 RBAC 的 `alert_config.manage` 管控，不再硬编码超级管理员；
6. CI 红线扫描在干净检出环境中扫描本次提交，而不是空工作区；
7. 记忆检索先在数据库过滤、后截断，并批量更新召回统计；
8. 默认 Playbook 只在迁移或显式初始化命令中补建，请求读路径不写库。

## 2. 数据范围机制

在 `DatabaseConfig` 上增加 `visible_to(user)` QuerySet 能力。超级管理员或未配置范围的
用户返回原 QuerySet；受限用户增加 `id__in=allowed_databases` 条件。Copilot 入口必须显式
接收 `user`，所有全局工具必须接收已经过滤的实例 QuerySet；与实例关联的 Alert、Baseline、
Memory 查询均从该集合派生。没有 `user` 的内部调用采用拒绝全局扫描的安全默认值，只有显式
传入 `config` 时允许读取该实例。

## 3. 真实观测契约

Copilot 工具统一返回以下状态字段：

```json
{"available": false, "reason": "暂无采集数据", "observed_at": null}
```

- ASH：读取时序库最近 120 秒 `session_sample`，只汇总真实样本；
- 表空间：只解析最新 `MonitorLog` 中的 `tablespaces/tablespace_usage`；
- 执行计划：只读取 `SqlPlan` 已采集快照，并用 `IndexAdvisor` 对随存 SQL 原文给出规则建议；
- 动作卡片：仅在真实观测结果 `available=true` 且存在明确对象时生成；无事故证据时不生成
  kill/扩容执行卡片，只保留性能中心导航；
- Prompt 明确规定不可用状态不得被推测、补齐或改写为事实。

## 4. LLM 凭据迁移

### 4.1 模型

`LLMProviderCredential.api_key` 长度从 255 扩到 512，以容纳 `enc:` 加随机 nonce、认证标签
后的 Base64 密文。模型提供 `set_api_key()` / `get_api_key()`，`save()` 对非 `enc:` 值幂等加密。

### 4.2 数据迁移

新增 `0030`：

1. 先扩字段；
2. 对非空且非 `enc:` 的存量 Key 逐行调用现有 AES-GCM 加密函数；
3. 补建缺失的三条兼容 PlaybookTemplate，使用 `get_or_create`，不覆盖 DBA 定制；
4. 反向迁移不解密密钥，避免回滚操作把密钥重新暴露为明文；仅保留数据。

部署前必须备份数据库并确认 `DB_MONITOR_SECRET_KEY` 在所有 worker 一致。若该密钥丢失，
密文不可恢复。迁移后抽查只能验证 `enc:` 前缀，严禁打印明文。

## 5. LLM 出站安全

新增统一校验器：Base URL 必须为 HTTPS、无 URL 内嵌凭据、无 fragment、端口为 443；解析后的
全部 IP 必须为公网地址。银行内网模型端点只能通过部署期 `LLM_ALLOWED_ENDPOINT_HOSTS` 精确
白名单放行，仍要求 HTTPS。Web API 不再接收 `proxy_url`；代理只允许由部署期
`LLM_PROXY_URL` 指定并由同一校验器检查。保存时校验一次，Provider 每次构造时再次校验，
防止绕过 API 直接写库。

## 6. API 与兼容策略

- LLM 配置 GET 保持现有响应结构，优先展示数据库中的系统默认凭据；
- PUT 不再修改进程内 settings 或 `.env`，改为创建/更新 `系统默认` 凭据；
- 凭据、路由、连通性测试的 GET 使用 `alert_config.view`，POST/PUT/DELETE/PING 使用
  `alert_config.manage`；
- 存量 `proxy_url` 只在迁移后作为历史字段保留，运行时忽略，接口不再返回具体代理地址；
- settings 中的 LLM 配置仅作为只读部署级 fallback，不能由 Web 修改。

## 7. CI 红线门禁

本地存在未提交改动时继续扫描 `git diff HEAD`；CI 干净检出时扫描 `HEAD^..HEAD`。
Checkout 至少获取两层历史。`_changed_files()`、新增行解析和 migration 删除检查必须共用
同一个 diff 范围。另增加 Copilot 生产代码固定业务样例字面量检查，阻止样例事实再次进入工具层。

## 8. 测试与验收

1. 将 `docs/review/v2.5/tests_v25_review.py` 移入 `monitor/tests_v25_review.py`；
2. 扩充数据范围、无数据契约、真实样本、凭据迁移/解密、URL 防护、RBAC、记忆查询、
   Playbook 无读写副作用和 CI diff 范围测试；
3. 每次文件编辑后执行 `scripts/validate.sh unit --json`；
4. 最终执行 `scripts/validate.sh all`、安全扫描、迁移漂移检查及远端 CI；
5. 验收要求所有复现用例转绿，且报告目录不再保留不可执行的测试副本。
