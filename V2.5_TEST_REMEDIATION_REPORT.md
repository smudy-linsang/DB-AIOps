# DB-AIOps v2.5 多轮测试报告与测试整改方案

> 测试日期：2026-08-16  
> 基线：`master@6320e95`  
> 覆盖层次：Smoke、SIT、UAT 契约、数据库方言、静态安全、依赖安全、覆盖率、性能基线

## 1. 总体结论

基线测试结论为：**开发基础闸门可通过，集成和生产就绪性不通过**。

- 单元闸门、Django 基础测试、前端 Vitest/build 通过。
- MySQL/PostgreSQL/Oracle 真实方言用例通过 19 条、跳过 2 条达梦环境用例。
- Phase 6b、Phase 7、Phase 8 存在稳定失败，核心原因分别是诊断保存字段错误、时序 schema 漂移、PG 扩展缺失。
- 7 条 V2 复审用例仍被标记为 expected failure，等价于已知缺陷没有关闭。
- 生产依赖安全审计、前端静态质量和核心覆盖率未达到上线门槛。

## 2. 环境与数据

| 组件 | 测试环境状态 | 备注 |
|---|---|---|
| Django | 本地虚拟环境可运行 | 本机解析到 Django 6.0.5，CI Py3.11 会解析 5.2 |
| MySQL | healthy | 方言测试通过 |
| PostgreSQL | healthy | 5433 被监控库缺 pg_stat_statements |
| Oracle XE | healthy | 方言测试通过 |
| TimescaleDB | healthy | 运行 schema 缺 `wait_secs/trx_age_secs` |
| Redis | healthy | 应用生产配置契约不一致 |
| Elasticsearch | healthy | 开发环境关闭安全特性 |
| GBase | unhealthy | 未具备可信实库验收条件 |
| 达梦 | 未提供 | 仅可执行降级/契约测试 |
| TDSQL | healthy | 复用 MySQL 协议，需补独立特性矩阵 |

## 3. 第一轮 Smoke 结果

| 编号 | 测试 | 结果 | 证据/发现 |
|---|---|---|---|
| SMK-01 | `scripts/validate.sh unit --json` | 通过 | 退出码 0，约 4 秒 |
| SMK-02 | Django backend | 通过但有风险 | 208 tests，21 skipped，7 expected failures；一次真实 LLM 调用约 15 秒 |
| SMK-03 | 前端 Vitest | 通过 | 2 个文件、12 条测试 |
| SMK-04 | 前端生产 build | 通过但不达预算 | ECharts 约 676KB，AntD 约 1.177MB |
| SMK-05 | 容器健康 | 部分失败 | GBase unhealthy |
| SMK-06 | 应用健康接口 | 误通过 | 缺 sentinel/pipeline、重复 collector 时仍报告 healthy |
| SMK-07 | 运行进程唯一性 | 失败 | 同时存在 2 个 runserver、2 个 start_monitor |
| SMK-08 | 关键后台角色 | 失败 | 未发现 sentinel 和 pipeline |

### Smoke 缺陷

- TST-001：测试环境没有强制禁用外部 LLM，测试不确定、慢且可能产生费用/数据外发。
- TST-002：健康接口没有覆盖完整后台角色，也不能识别重复 leader。
- TST-003：前端测试仅覆盖少量组件，没有覆盖 WarRoom、自愈确认和越权错误。
- TST-004：构建产物没有 bundle budget，依赖升级可能无声推高首屏成本。

## 4. 第一轮 SIT 结果

| 套件 | 结果 | 通过率 | 失败原因 |
|---|---:|---:|---|
| Phase 5 | 通过 | 20/20 | — |
| Phase 6a | 部分通过 | 10/11 | MySQL 瞬时断连；TSDB 查询暴露缺列和游标二次 yield |
| Phase 6b | 失败 | 3/6 | `plans` 不是具体字段，诊断保存 ValueError |
| Phase 6c | 通过 | 7/7 | — |
| Phase 7 | 失败 | 5/8 | TSDB 缺列、近期样本不足、PG 缺扩展 |
| Phase 8 | 阻断 | 首场景失败 | 同一 `plans` 保存缺陷阻断后续 |
| Phase 7 手工契约断言 | 通过但未纳管 | 43/43 | 文件未被 Django 自动发现 |

### SIT 缺陷

- TST-005：部署数据库 schema 与代码版本不匹配，且启动未检查。
- TST-006：核心诊断流水线没有由自动测试覆盖到真实保存路径。
- TST-007：上下文管理器错误路径缺少单测，二次 yield 掩盖根因。
- TST-008：验证脚本遇首个异常即退出，不能一次收集完整结果。
- TST-009：PG 扩展等纳管先决条件没有预检/清晰降级。
- TST-010：手工契约的 43 条断言不属于默认测试套件，CI 绿灯不代表它们执行。

## 5. 数据库方言与兼容性测试

以真实 MySQL、PostgreSQL、Oracle DSN 执行 21 条方言用例，19 通过、2 条达梦用例因环境缺失跳过。该结果证明三种主流方言的当前样例路径可用，但不能替代以下矩阵：

| 维度 | 必测组合 |
|---|---|
| 版本 | MySQL 5.7/8.0、PG 12/14/16、Oracle 11g/19c、达梦 8、GBase、TDSQL 已支持版本 |
| 权限 | DBA、最小只读权限、视图/扩展缺失 |
| 网络 | 连接拒绝、DNS 慢、TLS 错、查询超时、半开连接 |
| 数据 | 空库、高基数、长 SQL、非 ASCII、超大数值、时区/DST |
| 角色 | 主库、备库、只读节点、集群切换期间 |

`pyodbc` 在缺 unixODBC 动态库的机器上导入失败；必须按可选能力处理，不能拖累其他数据库类型。

## 6. UAT 场景评审

基线未具备正式业务方签字条件。按银行 DBA 日常值守定义 12 条核心旅程，当前评估如下：

| UAT-ID | 业务旅程 | 基线结论 | v2.5 验收标准 |
|---|---|---|---|
| UAT-01 | 新实例纳管与最小权限校验 | 部分具备 | 5 分钟内完成；缺权限准确到视图/语句 |
| UAT-02 | 指标断流识别 | 不通过 | 2 个采集周期内告警，不能显示健康 |
| UAT-03 | 阻塞根会话定位 | 不通过 | 返回实时会话与证据，不得伪造 |
| UAT-04 | 慢 SQL 诊断 | 部分具备 | 指标、SQL 指纹、执行计划、建议可追溯 |
| UAT-05 | 事故自动 RCA | 不通过 | 无硬编码结论；证据不足明确标示 |
| UAT-06 | L1 建议、L2 审批执行 | 不通过 | 自治等级、审批、幂等、回滚全部生效 |
| UAT-07 | 受保护账户/核心库阻断 | 不通过 | 服务端识别，不信任客户端用户名 |
| UAT-08 | 越权用户访问其他实例 | 不通过 | 查询、预演、执行均 404/403 且审计 |
| UAT-09 | 目标库执行失败与重试 | 不通过 | 不得记成功；错误分类并可安全重试 |
| UAT-10 | 巡检报告生成 | 部分具备 | 数据时效、缺失项、口径和版本写入报告 |
| UAT-11 | 主备切换期间监控连续性 | 未验证 | 自动重新解析角色，避免双写/重复告警 |
| UAT-12 | 平台组件故障 | 不通过 | readiness 降级，告警到平台维护人员 |

## 7. 安全与静态测试

### 7.1 Python

- Ruff：2,643 项，其中宽泛异常 772、try/except/pass 182、未使用 import 231，并发现 8 处未定义符号。
- Bandit：233 项（5 high、26 medium、202 low）。大量 hash 告警属于标识用途，但任意 webhook 目标和动态 SQL 必须按语义整改。
- pip-audit：应用环境发现 Django 与 cryptography 共 12 项漏洞；工具 pip 另有 2 条重复报告。

### 7.2 前端

- Biome：122 errors、186 warnings；除格式项外包含 Hook 依赖、数组 index key、回调返回语义和重复对象键。
- npm audit：2 high、4 moderate，涉及 Axios/form-data、ECharts XSS、React Router/open redirect 等依赖链。
- 报告 HTML 已使用 DOMPurify，后续必须保留安全回归测试，不能为消除静态告警而取消净化。

## 8. 覆盖率结果

关闭外部集成后运行单元覆盖率：106 条测试约 3.5 秒，总覆盖率约 **26%**。`diagnosis_pipeline`、`incident_manager`、`playbook_engine`、`playbook_authz`、`report_engine`、`inspection_executor` 等核心模块存在 0% 或极低覆盖。

覆盖率整改门槛：

- 访问控制、自愈执行、审批、审计、时序连接、诊断状态机：行覆盖 >=80%，分支覆盖 >=70%。
- 所有新增/修改代码：行覆盖 >=85%。
- 全仓先设 35% 防回退，再按版本提升到 50%/65%，不以大量低价值测试粉饰数字。
- expectedFailure 不计作关闭；每条必须转为普通通过或删除被淘汰功能。

## 9. 性能基线与缺口

本轮完成了构建体积、测试耗时、进程资源和时序数据规模的基线观察，但基线环境存在功能阻断，尚不能给出“性能通过”结论。已发现：

- 一个 collector 进程自 8 月 9 日累计 CPU 时间异常高，且存在双实例，测得吞吐会被重复采集污染。
- 核心采集函数过长、串行查询多，难以做查询级预算和超时归因。
- 连续聚合有较大历史数据，但没有保留/压缩/磁盘水位验收。
- 前端主依赖 chunk 超过 500KB 警戒线。

v2.5 性能验收模型：

| 场景 | 负载 | 指标门槛 |
|---|---|---|
| API 查询 | 50 并发、持续 15 分钟 | p95 <500ms，p99 <1s，错误率 <0.1% |
| WarRoom 聚合 | 20 并发、1,000 事故历史 | p95 <1.5s，无 N+1 |
| 采集调度 | 500 实例、60s 周期 | 95% 在周期内完成，无重复采集 |
| 告警风暴 | 10,000 事件/5min | 去重后无丢失，队列恢复 <10min |
| 时序写入 | 每秒 5,000 样本 | p95 <200ms，连接池无泄漏 |
| 报告生成 | 100 实例/30 天 | <120s，内存峰值 <1GB |
| 前端 | 常用路由冷加载 | gzip 首屏 JS <500KB，LCP <2.5s（内网标准终端） |

## 10. 测试体系整改

### 10.1 分层

1. Unit：纯函数、状态机、权限判定、方言结果转换，零外部依赖。
2. Contract：各数据库查询模板与结果 schema；驱动异常和版本差异。
3. Integration：PostgreSQL 行锁、Timescale、Redis、真实目标库。
4. SIT：从采集到事故、RCA、审批、执行、验证、关闭的端到端链路。
5. UAT：按 12 条 DBA 业务旅程，由角色账户签字。
6. NFR：容量、故障注入、安全、恢复、长稳。

### 10.2 自动化门禁

- `unit` 默认关闭 LLM、Agent、Embedding、外部通知。
- 将 43 条 Phase 7 断言改成自动发现测试。
- CI 增加 schema 漂移、Ruff 高风险规则、Bandit 高置信、pip/npm audit、前端 bundle budget。
- 性能使用固定数据集和阈值，结果输出 JSON/JUnit 并保留趋势；共享 runner 只做回归，小于门槛不用于容量承诺。
- 实库测试每夜运行；Oracle/达梦/GBase 缺环境必须明确为“未验证”，禁止显示绿色通过。

## 11. 复测记录模板

整改后每个缺陷必须回填：提交号、测试环境、命令、开始/结束时间、结果、关键指标、日志/报告位置、执行人、复核人。最终放行至少包含：

- 单元闸门连续 3 次通过。
- backend/frontend/all 通过。
- Phase 5～8 全部完成，不因首错中止。
- MySQL/PG/Oracle 方言测试通过；达梦/GBase/TDSQL 有环境的独立证据。
- 12 条 UAT 旅程通过。
- 15 分钟基准、2 小时稳定性和故障恢复测试达标。
- 依赖审计无 critical/high，数据范围反向测试全部通过。

## 12. v2.5 整改后复测实录

以下结果为同日整改后的实际执行记录，不覆盖前文基线失败事实。

| 复测项 | 结果 | 关键证据 |
|---|---|---|
| 强制 unit 门禁 | 通过 | 每次编辑后执行，最终连续通过；无 expectedFailure |
| Django 全量回归 | 通过 | 最终 240 tests 全通过，21 条仅因未提供外部商业库环境而条件跳过 |
| Phase 5 | 通过 | 20/20 |
| Phase 6b | 通过 | 6/6，`plans` 4NF 保存已修复 |
| Phase 7 | 通过 | 8/8，Timescale schema 与 PG 扩展补齐 |
| Phase 8 | 通过 | 22/22，案例 tags 4NF 保存已修复 |
| MySQL/PG/Oracle 方言 | 通过 | 19 条实跑通过；达梦 2 条因无环境明确跳过 |
| Python 确定性静态错误 | 通过 | Ruff F821/F601/F811=0 |
| Python 高危安全模式 | 通过 | Bandit high=0；5 处弱摘要升级 SHA-256 |
| Python 依赖漏洞 | 通过 | 完整哈希锁 44 个包，pip-audit 0 已知漏洞 |
| 前端依赖漏洞 | 通过 | npm audit 0 vulnerability |
| 前端 build/Vitest | 通过 | Vite 8.2.1、Vitest 4.1.10；构建和 12 条测试通过 |
| 生产镜像 | 通过 | 前后端多阶段镜像均构建成功；后端 UID/GID 10001、Django 5.2.17 |
| 租约抢占 | 通过 | PostgreSQL 实测 A token=1、B 同期拒绝、释放后 B token=2；测试行已清理 |
| 覆盖率 | 达到首阶段线 | 全仓 35.2%；通知 81.3%、执行桥 81.8%、租约 77.9%、RCA v3 78.7%，四模块合计 79.9% |
| 生产配置/编排 | 通过 | Compose 解析通过，`check --deploy` 零告警，固定镜像 digest、非 root、回环监听和专用可信代理 CIDR 生效 |
| Webhook 出站安全 | 通过 | HTTPS/官方域名/443/无凭据 URL；30x 拒绝、64 KiB 响应上限、分段超时、令牌安全日志均有测试 |

最终镜像证据：后端用户 `dbmonitor`、Gunicorn 3×8 gthread，前端用户 UID 101、Nginx 配置检查通过。由于报告自身也进入构建上下文，应用镜像摘要会随报告回填而变化；正式摘要以发布后的 `docker inspect` 和行内仓库签名记录为准，不在镜像内自引用固定值。

### 12.1 三轮性能整改复测

环境为本机 3 worker × 8 gthread Gunicorn；每轮独立预热，50 并发。该结果用于代码回归，不作为生产容量承诺。

| 端点 | 每轮请求 | 三轮错误率 | p95 范围 | 吞吐范围 |
|---|---:|---:|---:|---:|
| `/livez` | 2,000 | 0% | 22.38–32.00 ms | 3,370–3,637 RPS |
| `/readyz` 整改前 | 1,000 | 0% | 503.88–1,453.81 ms | 140–232 RPS |
| `/readyz` 整改后 | 1,000 | 0% | 250.55–274.27 ms | 342–370 RPS |

`/readyz` 性能缺陷根因为时区字段使用 naive datetime 导致高并发日志风暴，以及同步 worker 并发槽不足。整改为 `timezone.now()` 和 3×8 gthread 后，三轮 p95 均低于 500 ms 门槛。

### 12.2 尚需银行环境签字的项目

以下不是可在单机开发环境内伪造“通过”的项目：达梦/GBase/TDSQL 指定版本实库矩阵、500 实例 60 秒采集容量、10,000 事件风暴、15 分钟持续 API 压测、2 小时长稳、主备/机房切换、备份恢复、IAM/MFA/CMDB/工单/WORM 集成，以及 DBA、应用、信息安全、审计、运维平台五方 UAT。它们在取得对应环境和人员后执行，未执行前不得作为银行生产正式放行证据。
