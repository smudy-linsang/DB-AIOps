# DB-AIOps v2.5 独立复审整改与复测报告

## 1. 整改结论

本报告对应 `V2.5_INDEPENDENT_REVIEW.md` 的 R25-01 至 R25-12。报告中的 3 个 P0、
4 个 P1、5 个 P2 已全部完成代码整改；原先暂存在 `docs/review/v2.5/`、不会被 CI
执行的 6 条失败用例已迁入 `monitor/tests_v25_review.py`，并扩展为 16 条正式回归用例。

整改原则：越权与不存在统一对外隐藏；无证据即返回不可用；凭据只以密文落库；
写操作只认 RBAC；配置只走单一事实源；读路径不做初始化写入；门禁必须在 CI 中真实执行。

## 2. 逐项闭环

| 编号 | 原问题 | 整改结果 | 主要实现 |
|---|---|---|---|
| R25-01 | Copilot 全局工具越权 | 已关闭 | `DatabaseConfig.objects.visible_to(user)` 成为统一范围原语；Copilot 入口显式接收 user；资产、告警、基线、表空间、记忆均从可见实例集合派生；无 user 的内部调用禁止全局扫描 |
| R25-02 | ASH/计划/表空间编造事实 | 已关闭 | ASH 只读最近 120 秒真实 `session_sample`；计划只读已采集 `SqlPlan`；表空间只读最新 MonitorLog；无数据统一 `available=false`，Prompt 明令禁止推测 |
| R25-03 | LLM Key 明文落库 | 已关闭 | `api_key` 扩至 512；模型 save 防线自动 AES-256-GCM；`0030` 加密存量 Key；Router/Ping/API 统一调用 `get_api_key()` |
| R25-04 | config 赋值顺序错误 | 已关闭 | 入口先解析调用者可见 QuerySet 和选中实例，再触发任何工具；运行时回归验证只返回选中实例 |
| R25-05 | base/proxy URL 无防护 | 已关闭 | 保存及调用双层校验 HTTPS、443、无内嵌凭据/fragment、DNS 解析后拒绝非公网地址；内网模型需部署白名单；Web 代理停用并清理存量值；禁重定向 |
| R25-06 | CI 红线扫描空转 | 已关闭 | 本地扫描工作区，CI 干净检出扫描 `HEAD^..HEAD`；checkout 深度改为 2；三类原门禁共用同一 diff；新增 Copilot 固定业务样例字面量门禁 |
| R25-07 | 记忆检索失真与 N 次写库 | 已关闭 | 关键词 `Q()` 过滤先下推数据库再排序截断；中文 2-4 字滑窗；召回计数用单条 `F()` 批量更新；受限用户不读取他库或全局记忆 |
| R25-08 | VIEW 装饰器承载写接口 | 已关闭 | GET 使用 `alert_config.view`；POST/PUT/DELETE/PING/连通测试使用 `alert_config.manage`；删除 handler 内超级管理员硬编码 |
| R25-09 | Web 改 settings 与 `.env` | 已关闭 | 全局配置 PUT 改为更新“系统默认”数据库凭据及 `global_default` 路由；不修改 worker 内存或部署文件；所有 worker 下次查询统一生效 |
| R25-10 | ReAct 名称与实现不符 | 已关闭 | 统一更名为“关键词证据路由 / Evidence Router”，前后端不再宣称 LLM 多轮 ReAct 工具决策 |
| R25-11 | Copilot 硬编码处置目标 | 已关闭 | 删除固定会话、用户、SQL、表空间及 kill/扩容执行卡片；聊天层只提供安全导航，执行必须从事故证据和审批链进入 |
| R25-12 | Playbook 请求读路径初始化 | 已关闭 | `evaluate_dryrun()` 不再调用 bootstrap；缺失模板明确拒绝；默认模板由 `0030` 数据迁移幂等补建且不覆盖 DBA 定制 |

## 3. 迁移与安全说明

迁移 `monitor/migrations/0030_v25_review_hardening.py` 按以下顺序执行：

1. 扩大 API Key 字段，避免 AES-GCM 密文截断；
2. 将所有非空、非 `enc:` 的存量 Key 加密；
3. 清空已停用的 Web 代理字段；
4. `get_or_create` 补建 PlaybookTemplate 和 LLM 路由，不覆盖现有定制；
5. 反向迁移不解密 Key，防止回滚重新产生明文。

投产迁移前必须确认所有容器使用同一个 `DB_MONITOR_SECRET_KEY` 并完成数据库备份。
该密钥轮换不能通过直接替换环境变量完成，必须执行解密旧值、使用新密钥重加密的受控流程。

Gemini 调用同时完成额外加固：API Key 只放 `x-goog-api-key` 请求头，不再拼入 URL，
避免进入代理访问日志；LLM POST 禁止自动跟随重定向。

## 4. 回归覆盖

`monitor/tests_v25_review.py` 覆盖：

- 资产清单和 QuerySet 数据范围；
- 记忆及选中实例隔离；
- ASH 无数据和真实样本两种契约；
- 表空间、执行计划不得编造；
- 模型 save 与存量数据迁移加密；
- LLM SSRF、部署白名单、Gemini Header-only Key；
- VIEW/MANAGE 权限分离和多 worker 配置事实源；
- CI 干净检出 diff 选择；
- Playbook 预演无初始化写副作用。

最终验收命令：

```bash
scripts/validate.sh unit --json
scripts/validate.sh all
venv/bin/python manage.py test monitor --shuffle -v 1
venv/bin/ruff check monitor dbmonitor scripts --select F821,F601,F811
venv/bin/bandit -q -r monitor dbmonitor -lll -iii
venv/bin/pip-audit -r requirements.lock --require-hashes
cd frontend && npm audit --audit-level=high
```

## 5. 放行边界

本次闭环解决的是独立复审报告内的软件缺陷。行内模型网关证书、DNS、出站防火墙、
Secret Manager/HSM 注入、密钥轮换演练和五方 UAT 仍属于银行环境投产证据，不能用
单元测试替代。`LLM_ALLOWED_ENDPOINT_HOSTS` 必须填写精确域名，不得配置通配符或网段。
