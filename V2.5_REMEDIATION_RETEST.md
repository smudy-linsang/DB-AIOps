# v2.5 整改复测报告

**复测对象**：`2b863fb fix(v2.5): 完成独立复审问题整改`（21 个文件，+1100 / −494）
**复测日期**：2026-08-18
**复测方式**：不采信整改方自带的测试 —— 逐条读修复后代码 + 用我原始用例原样回放 + 独立功能探针
**基线环境**：Python 3.11 / Django 5.2.17 / PostgreSQL 16（本地真实实例）

---

## 0. 结论

**`V2.5_INDEPENDENT_REVIEW.md` 里的 12 项问题全部真修复，没有一项是靠改测试蒙混过去的。**
其中 R25-01（数据范围）按我建议的根因方案做了 —— 引入 `visible_to` QuerySet 原语，
而不是在出问题的那几个函数上打补丁。

复测中另外**新发现 1 项既有越权缺陷**（不是本次整改引入），见 §3。

| | 结果 |
|---|---|
| 后端全量 | 256 passed / 21 skipped |
| 随机顺序复跑 | OK |
| 我的原始复审用例原样回放 | 6 条中 5 条转绿；剩下 1 条是**我自己的测试写坏了**（见 §2） |
| 静态门禁（check / 迁移漂移 / 红线 / 密钥 / 依赖 / ruff） | 全部通过 |
| 前端 build / vitest | 通过 / 12 passed |

---

## 1. 逐条核验（方法与证据）

我对每一条都做了**独立于整改方测试**的验证。测试绿不算数 —— 一个断言
`available == False` 的用例，用一个永远返回 False 的桩也能满足。

### R25-01 数据范围越权 → ✅ 按根因修复

新增 `DatabaseConfigQuerySet.visible_to(user)`：`user is None` 时返回 `none()`（fail-closed），
无限制用户返回全集，其余按 `get_user_database_ids` 过滤。

`run_copilot_chat(..., *, user=None)` 改为关键字参数接收调用者，
一次算出 `visible_configs` 传给所有工具；`config` 也从 `visible_configs` 里解析
（即用户显式选中的实例同样过范围校验）。无 user 且无 config_id 时退化为 `none()` 而非全局。

**独立探针**（不是跑他们的测试）：

```
只授权 mine 的用户提问"我们纳管了哪些库？"
  → 资产清单 = ['sel-mine']            （整改前会带出 10.88.88.88 的生产库）
```

### R25-02 工具层编造事实 → ✅ 真删了，且不是桩

`get_realtime_ash()` 现在从 `get_timeseries_storage().query_session_samples()`
读最近 120 秒真实样本，无样本时返回 `available=False` + 明确 reason；有样本时
从真实行计算 `blocked_session_count` / `top_sessions`。表空间、执行计划同构。

全仓库搜索旧的编造固定值（`trade_order` / `app_trade_user` / `1845` / `8a7fbc6d` /
`18450` / `90.3` / `86.3`）：**在 copilot.py 与 api_views_v2.py 中已全部清除**。

### R25-03 API Key 明文落库 → ✅ 已加密，且带存量迁移

用我自己的原始 SQL 探针复验：

```
落库值       : enc:cmlFUn+Vmb6khbk7byUbjWV3kIJ1
是否明文     : False
get_api_key():  与原文一致（往返正确）
```

迁移 `0030` 会把存量明文 Key 就地加密，并清空历史 Web 写入的 `proxy_url`。

### R25-04 config 赋值前被引用 → ✅ 顺序已修

`config` 现在在所有工具触发之前解析。**功能探针**（非源码文本断言）：

```
选中 mine(id=1) 后提问表空间 → 工具覆盖的 config_id = [1]   （整改前是全局扫描）
```

### R25-05 LLM 端点无 SSRF 校验 → ✅ 新增 `monitor/llm/security.py`

强制 https、解析 DNS 后逐个校验 `is_global`（拦私网/回环/链路本地/保留地址）、
拒绝 URL 内嵌凭据与非 443 端口，内网端点须由**部署白名单**显式放行。
且**保存时与真正发请求前都校验**（`providers.__init__` 里也调），
所以绕过 API 直接写库也无效。`proxy_url` 改为部署期配置，Web 不可写。

顺带修了一处我没提的：Gemini 的 key 从 URL query 改到 `x-goog-api-key` 头，
避免 Key 进日志与 Referer。

### R25-06 红线门禁在 CI 里空转 → ✅ 已真正生效（我重做了同一个实验）

新增 `_diff_args()`：工作区有改动就扫工作区；干净检出且 `CI` 环境变量存在时扫
`HEAD^..HEAD`。CI 配套加了 `fetch-depth: 2`。

**我用与上轮完全相同的手法复测** —— 克隆仓库、植入一处 `except: pass`、提交、
保持工作区干净：

```
CI 未设置        → 红线约束扫描通过        退出码=0
CI=true（真实 CI 条件） → 发现违规 monitor/degrade.py:45   退出码=1
```

上一轮这个实验的结果是"两种情况都退出 0"。现在门禁真的能拦住了。

### R25-07 记忆召回失真 + 读路径写库 → ✅

过滤下推到数据库（`Q(icontains)` 组合）后再排序取 5；访问计数改为一次
`update(access_count=F('access_count')+1)` 批量写。另外补了中文 2-4 字滑窗切词，
使"回忆容量经验"能匹配到"容量" —— 这一点比我建议的更细致。

### R25-08 / 09 / 10 / 11 / 12 → ✅ 全部处理

| 项 | 核验 |
|---|---|
| R25-08 RBAC | 写端点改用 `ALERT_CONFIG_MANAGE`；`is_superuser` 硬编码**已全部移除** |
| R25-09 运行时改 settings / 写 .env | `settings.LLM_*=` 与 `.env` 写入代码已删除 |
| R25-10 "ReAct" 名不副实 | 相关宣称已从代码与文档移除，注释改为"关键词证据路由" |
| R25-11 硬编码 kill 参数 | 已删；该分支现返回"预演必须从已授权事故进入" |
| R25-12 种子在读路径 | `init_default_playbooks()` 已无人调用；种子移入迁移 `0030` 并用 `get_or_create`（保留 DBA 自定义）。实测迁移后 `PlaybookTemplate` 3 条、路由规则 5 条，功能未因此失效 |

---

## 2. 更正：我自己的一条复审用例是坏的

回放我的原始用例时，`test_tablespace_tool_respects_selected_database` 报失败。
**这是我的测试写错了，不是他们的代码没修。**

我当时图省事用了源码文本断言：`src.split('if config_id:')[0]` 再查子串。
整改把函数结构重写之后，那个 `if config_id:` 锚点不存在了，split 返回整段源码，
于是必然"命中"。换成功能探针后确认行为完全正确（见 §1 R25-04）。

教训与我这几轮一直在讲的是同一条：**断言要盯住行为，不要盯住源码长相**。
我自己也踩了一次。

---

## 3. 复测中新发现的问题（既有缺陷，非本次整改引入）

### RT-01【安全】容量总览与拓扑总览无权限、无数据范围

**定位** `monitor/api_views.py` — `CapacityOverviewView`、`TopologyOverviewView`

两个视图的 dispatch 只有 `@require_auth`，**既没有 `require_permission`，也没有任何数据范围过滤**：

```python
class CapacityOverviewView(JSONResponseMixin, View):
    @method_decorator(csrf_exempt)
    @method_decorator(require_auth)      # ← 仅此而已
    def dispatch(self, *args, **kwargs): ...
    def get(self, request):
        configs = DatabaseConfig.objects.filter(is_active=True)   # 全表
```

**实证**：用一个**零权限、数据范围为空**的 READONLY 账号请求：

```
/api/v1/capacity/overview/    status=200  泄露范围外实例=是
/api/v1/topology/overview/    status=200  泄露范围外实例=是
```

泄露内容包括全部纳管实例的**名称与主机地址**。

**说明**：这两处在 `api_views.py`，本次整改未触碰该文件，属**既有缺陷**；
我前几轮聚焦在各版本的增量 diff 上，没有扫到它们，这次是顺着
"`visible_to` 到底被采用了多少"才发现的。

**复现用例**：`docs/review/retest/tests_retest_scope.py`（2 条，当前均失败）。

---

## 4. 残留观察（不影响放行，建议排期）

1. **`visible_to` 目前只有 1 处调用点**（`copilot.py:463`）。
   根因原语建好了，但尚未推广；`api_views.py` 里仍有多处请求路径直接全表取实例
   （RT-01 即其中两处）。建议做一次系统性替换，并考虑加一条静态检查：
   请求处理路径中出现裸 `DatabaseConfig.objects.filter/all` 即告警。
   否则"第四次复发"的结构性条件依然存在。

2. **红线门禁对多提交推送只覆盖最后一次提交**。`HEAD^..HEAD` + `fetch-depth: 2`
   意味着一次 push 三个 commit 时，只有最后一个被扫。PR 场景因为 GitHub 给的是
   merge commit（`HEAD^` 即 base）反而是全量的。建议 push 场景改用
   `${{ github.event.before }}..HEAD` 并相应放宽 fetch-depth。

3. **`init_default_playbooks()` 成了死代码**（无任何调用点）。功能上没问题
   （种子已移入迁移），但留着会让人以为还有运行期 bootstrap。建议删除。

---

## 附录：本次复测执行的验证

| 验证 | 结果 |
|------|------|
| `manage.py test monitor` | 256 passed / 21 skipped |
| `manage.py test monitor --shuffle` | OK |
| 我的原始复审用例原样回放 | 5/6 转绿；1 条系我自己的源码文本断言失效 |
| 独立功能探针：资产清单范围 | 只返回授权实例 |
| 独立功能探针：选中实例后表空间工具范围 | 只覆盖选中实例 |
| 原始 SQL 探针：`llm_provider_credential.api_key` | `enc:` 密文，往返正确 |
| 克隆仓库重做红线门禁实验 | `CI=true` 下成功拦截（上轮为放行） |
| 迁移后种子数据 | PlaybookTemplate 3 条 / 路由规则 5 条 |
| 旧编造固定值全仓搜索 | copilot.py、api_views_v2.py 已清零 |
| `manage.py check` / 迁移漂移 / 红线 / 密钥 / 依赖 / ruff | 全部通过 |
| 前端 build / vitest | 通过 / 12 passed |
| **新发现** 零权限账号访问容量与拓扑总览 | **泄露全部实例名称与主机** |
